# Komagin HR — Master Remediation Register

**Document type:** Phase 0 Baseline Deliverable #8 of 9 — updated at the close of Phase 1, Phase 2, Phase 3, and progressively during Phase 4
**Status:** Living document. Phase 0 rows are the original baseline; rows touched by Phase 1 (Security Foundation & Authorization Framework), Phase 2 (Authentication, Session Security & Portal Hardening), Phase 3 (Database Schema Integrity, Migration Safety & Data-Layer Hardening), or Phase 4 (Business Workflow Integrity, Module Consistency & Process Hardening — in progress) are marked Fixed/Verified below with evidence in the respective phase's Regression/Testing/Workflow reports.
**Date compiled:** 2026-07-11 (Phase 0) — updated 2026-07-11/12 (Phase 1 close) — updated 2026-07-11/12 (Phase 2 close) — updated 2026-07-12 (Phase 3 close) — updated 2026-07-13 (Phase 4 close) — **updating 2026-07-13 (Phase 5, in progress — Stage 5.1)**
**Baseline tag:** `v1.0-enterprise-baseline` → Phase 1 on branch `phase-1-authorization-framework` → Phase 2 on branch `phase-2-authentication-session-security` → Phase 3 on branch `phase-3-database-schema-integrity` → Phase 4 on branch `phase-4-business-workflow-integrity` → **Phase 5 on branch `phase-5-workflow-completeness-automation`**

---

## Source Documents

This register consolidates findings from three sources into one deduplicated list, each finding appearing exactly once:

1. **Audit I** — `Komagin_HR_Audit_Report_2026-07-10.html` (baseline independent audit, 34 findings: C-01–04, H-01–12, M-01–09, L-01–09)
2. **Audit II** — `Komagin_HR_Audit_Followup_2026-07-10.html` (same-day follow-up: regression-check of Audit I + 21 new findings: NC-01–02, NH-01–05, NM-01–07, NL-01–07)
3. **Phase 0 baseline inventory** (this program, 2026-07-11) — 12 newly discovered items surfaced while documenting the system, not previously reported by either audit

Where a finding was reported independently by more than one source (e.g. document-generation permission gating, reported as both `H-06` and `NM-04`), the two source IDs are merged into a single register entry with one final severity assessment, noted in the **Source ID(s)** column.

## Uniform Fields (apply to every finding unless a row overrides them)

Per Phase 0 rules, **no finding has been remediated yet.** Unless a row states otherwise:
- **Completion Status:** Not Started
- **Verification Status:** Not Verified
- **Date Closed:** — (none closed)
- **Regression Test Required:** Yes — see `Testing/09-baseline-test-plan.md` for the per-module test case that must pass before this finding can be marked Verified

## Target Phase Key

| Phase | Scope |
|---|---|
| **Phase 1** | Critical findings — live authorization bypasses, broken live features, DR/schema integrity |
| **Phase 2** | High findings — data exposure, privilege escalation, silent data-integrity failures |
| **Phase 3** | Medium findings — narrower-impact defects, policy/permission-granularity inconsistencies |
| **Phase 4** | Low findings — latent risk, dead code, hygiene, completeness gaps |
| **Phase 5** | Process/governance changes (not a single code fix — see Verification Report §4) |

---

## CRITICAL (13)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase | Verification Required |
|---|---|---|---|---|---|---|---|---|
| KOM-001 | C-01 | Approval engine has no approver-role authority check | Approvals | `modules/approvals/index.php`, `config/ApprovalEngine.php` | `index.php` gates with `requireLogin()` only, never `requirePermission()`; `ApprovalEngine::act()` never compares the acting user's role to the current stage's `approver_role` — any authenticated user can approve/reject any workflow. | **FIXED — Phase 1.** `act()` now requires `$actingUserRole`, checks role/assignee match, workflow state, stage state (duplicate-approval prevention), and separation of duties (initiator cannot self-approve, no super_admin exemption); throws `ApprovalAuthorizationException` on any violation and audits every attempt. `modules/approvals/index.php` updated to catch the exception. | Phase 1 | **VERIFIED live**: created a real leave workflow, confirmed the initiator was blocked from self-approving ("cannot approve or reject a workflow you initiated yourself"), confirmed a wrong-role user (hr_officer vs. required supervisor) was blocked ("not the assigned approver for this stage"), workflow status unchanged in both cases. See Regression Test Report. |
| KOM-002 | C-02 | Consultants module calls undefined CSRF function | Consultants | `modules/consultants/{add,edit,delete,scope_save}.php` | All four call `validateCsrfToken()`, which is never defined anywhere (correct helper is `verifyCsrfToken()`); every write throws a PHP Fatal Error. | **FIXED — Phase 2.** All four files now call `verifyCsrfToken()`. Full CRUD lifecycle live-tested: Add, Edit, Scope Save, and Delete all confirmed working end-to-end against real database records, including the cascading delete of dependent scope items. | Phase 2 | **VERIFIED live** — see Phase 2 Regression Test Report and Consultant Module Report for the complete Add→Edit→Scope Save→Delete evidence trail. |
| KOM-003 | C-03 | Kiosk allows remote clock-in impersonation | Attendance/Kiosk | `modules/attendance/kiosk.php` | With no `?t=` kiosk token, falls back to any globally-open kiosk session; no login, no CSRF, employee numbers are sequential/guessable; rate-limit only counts invalid-number attempts. | **FIXED — Phase 4.** Correctly out of scope for Phase 1 (an attendance-workflow/identification gap, not an authorization-consistency finding); addressed in Phase 4's Attendance & Timesheets workflow group. The no-token fallback is removed entirely — nothing legitimate relied on it (every kiosk location opened via `kiosk_manage.php` already has a token, and HR is always given the token-bearing URL). A missing/invalid token now shows "not a configured kiosk terminal" and performs no attendance action. | Phase 4 | **VERIFIED live**: temporarily opened one kiosk location; a request with no token was rejected on both the page-render and actual POST clock-in paths (confirmed zero attendance rows written); the correct token for that location worked normally. Kiosk restored to its original closed state afterward. See `Workflows/04-attendance-timesheets-workflow-report.md` §1. |
| KOM-004 | C-04 + Phase 0 DB inventory | Tracked SQL files cannot reconstruct a working database — 12 tables undefined, 1 broken forward-reference | Database / Schema | `database/schema.sql` and all `phase*.sql` files | 12 tables used by live code (`doc_templates`, `doc_categories`, `generated_documents`, `doc_template_versions`, `kiosk_sessions`, `kiosk_audit`, `employee_dependents`, `employee_qualifications`, `employee_work_history`, `approval_workflows`, `approval_stages`, `settings`) have no `CREATE TABLE` anywhere in the repo; `phase5_branding_theme.sql` ALTERs `doc_templates`, which no file ever creates — the tracked migration chain cannot be run end-to-end against an empty database. | **FIXED — Phase 3.** `database/schema.sql` rewritten from a verified `information_schema`-derived, topologically-ordered extraction of the live database (59 real tables + a new `schema_migrations` tracking table = 60), so every table any code path touches now has a tracked `CREATE TABLE`. `database/phase11_schema_reconciliation.sql` provides an idempotent upgrade path for any pre-Phase-3 database. 11 of the 12 tables resolved as genuine documentation gaps (structure extracted verbatim from live); the 12th, `settings`, resolved as a **phantom table that never existed anywhere, live or tracked** — `modules/activity_log/download.php` was the only reference, and it always threw an uncaught `PDOException` in production (see new finding KOM-070). | Phase 3 | **VERIFIED**: clean-install test (empty DB → 60/60 tables, 26/26 automated structural checks, full HTTP smoke test) and upgrade-migration test (live-DB clone → `phase11_schema_reconciliation.sql` → zero data loss across 10 critical tables, table count 59→60) both passed. See `Testing/12-phase3-clean-install-test-report.md` and `Testing/13-phase3-upgrade-migration-test-report.md`. |
| KOM-005 | NC-01 | temp_employees writes to schema-undefined columns | Temp Employees | `modules/temp_employees/{add,edit}.php`, `database/phase8_temp_employees.sql` | `add.php`/`edit.php` INSERT/UPDATE `rate_type` and `attendance_method`, neither defined in `phase8_temp_employees.sql`. | **FIXED — Phase 3.** Both columns added to `temp_employees` in `database/phase8_temp_employees.sql`'s `CREATE TABLE` (matching the live, already-working column definitions exactly) and to the canonical `database/schema.sql`. A fresh install now creates a fully functional `temp_employees` table on the first Add/Edit, not just the live database. | Phase 3 | **VERIFIED**: clean-install test confirms `temp_employees` has both columns (structural check) and that Add/Edit-equivalent queries execute without error. |
| KOM-006 | NC-02 | Branding asset folders' `.htaccess` blocked all letterhead/signature/stamp/watermark image serving — confirmed live | Documents / Branding | `uploads/{letterheads,signatures,stamps,watermarks}/.htaccess` | Flagged since the baseline audit as "not yet live-verified" and correctly fell out of scope for Phases 1–3; live-verified directly in Phase 4's Documents workflow group. All four folders carried an unconditional `Options -Indexes` / `Deny from all` (legacy Apache 2.2 syntax, no `<FilesMatch>` scoping at all) — confirmed this blocks every file in the folder, not just scripts: a disposable test image placed in `uploads/letterheads/` returned HTTP 403 on direct request. The entire document-branding feature (letterhead/signature/stamp/watermark images embedded by `DocumentEngine::wrapDocument()`) was completely non-functional on every generated document. | **FIXED — Phase 4.** All four `.htaccess` files rewritten to the same correctly-scoped pattern already used by the parent `uploads/.htaccess`: deny execution of script extensions only (`<FilesMatch>` on php/phtml/pl/py/cgi/sh/rb/asp/aspx/jsp), keep `Options -Indexes`, and otherwise allow static asset serving. | Phase 4 | **VERIFIED live**: a test image in each of the 4 folders now returns HTTP 200 (was 403 before the fix); a test `.php` file in the same folder still correctly returns 403 (script execution remains blocked) — confirmed `mod_access_compat` is loaded and the `Deny from all` inside `<FilesMatch>` genuinely works on this Apache 2.4 install, so the RCE-prevention protection is real, not just present in the file. Directory listing also confirmed still blocked. All test files removed after verification. |
| KOM-071 | Phase 4 — new | `employees.personal_email` referenced by 3 code paths, never existed anywhere — all 3 fatally broken | Employees / Self-Service | `modules/employees/edit.php`, `modules/employees/pending_updates.php`, `self-service/update.php` | No `personal_email` column existed in `schema.sql`, any tracked migration, or the live database. Every Edit Employee save, every self-service magic-link update page load, and approval of any `personal_email` pending-update request threw an uncaught `PDOException`. | **FIXED — Phase 4.** Column added to `employees` (`database/schema.sql` + new `database/phase12_workflow_integrity_fixes.sql`, applied live). | Phase 4 | **VERIFIED live**: all 3 code paths re-tested against a disposable test employee post-fix — Edit Employee save succeeds (302), self-service update page loads (200, no fatal error), pending-update approval writes correctly. |
| KOM-078 | Phase 4 — new | `positions` had no management UI anywhere in the app, and neither `departments` nor `positions` had any seed data in any tracked file | Departments / Positions | `modules/settings/index.php`, `database/seeds/003_departments_positions.sql` (new) | No `INSERT`/`UPDATE`/`DELETE` against `positions` existed anywhere in the codebase prior to this fix — the table could only ever be populated by a direct, untracked database edit. A fresh install had zero positions and no way to create one at all, permanently leaving the "Position" dropdown empty. `departments` had the identical seed-data gap (though it at least had a management UI). | **FIXED — Phase 4.** New Positions tab added to Settings (add/disable, duplicate-name guard); new seed file with the exact live 11 departments + 23 positions added to the install sequence. | Phase 4 | **VERIFIED live**: clean-install test 29/29 (3 new checks); added a real position via the new UI, confirmed duplicate rejection and disable-with-impact-count. |
| KOM-081 | Phase 4 — new | Rejected leave applications never restored `remaining_days` — permanent, silent balance loss | Leave | `modules/leave/approve.php` | `apply.php` reserves a request by decrementing `remaining_days` and incrementing `pending_days`. The reject branch only ever reversed `pending_days` — `remaining_days` was never credited back. Live-verified: a 2-day request reserved down to 7.0 remaining; after rejection, `pending_days` correctly returned to 0 but `remaining_days` stayed at 7.0 instead of 9.0. Every rejected application permanently shrank the real balance by the rejected amount. Directly contradicts the Phase 4 charter's explicit requirement to "ensure rejected requests restore balances correctly." | **FIXED — Phase 4.** Reject branch now also restores `remaining_days` (capped at `entitled_days` as a defensive guard). | Phase 4 | **VERIFIED live**: identical reject cycle now correctly returns both `pending_days` and `remaining_days` to their pre-application values. |
| KOM-049 | L-04 (severity corrected from Low to Critical — see reason) | Performance review creation completely broken — 3 nonexistent columns, not just the 1 originally reported | Performance | `modules/performance/save.php` | Originally reported (Phase 0) as "reads `overall_rating`; form posts `overall_score`" — a Low-severity single-field mismatch. Investigating it directly in Phase 4 revealed the `INSERT` actually named **three** columns that don't exist anywhere in `performance_reviews` at all (`overall_rating`, `comments`, `recommendations`) — not a silent data-loss bug but a hard crash on every single attempt. Live-verified: submitting the "Add Performance Review" form threw an uncaught `PDOException` on 100% of attempts. The entire feature had never worked. Severity corrected to Critical to reflect the true, fuller root cause. | **FIXED — Phase 4.** Corrected to the real column names: `overall_score` (form field already matched), `supervisor_assessment` for the form's "Comments" field, `recommendation_notes` for "Recommendations" — confirmed both are already displayed by `view.php`, so no UI change was needed. | Phase 4 | **VERIFIED live**: submitted a review with rating/comment/recommendation — saved successfully with all three values correctly present in the real columns (was: fatal crash on every attempt). See `Workflows/06-performance-management-workflow-report.md` §1. |
| KOM-087 | Phase 4 — new | No way to create a recruitment application existed anywhere in the app | Recruitment | `modules/recruitment/index.php`, `application_save.php` (new) | The only `INSERT INTO recruitment_applications` in the entire codebase was in demo seed data — `index.php` only lists/filters applications and `application_update.php` only changes an existing one's pipeline stage. The recruitment pipeline's very first step (a candidate applying) had no working entry point at all. | **FIXED — Phase 4.** New "Add Application" button/modal in `index.php` and new `application_save.php` handler, mirroring the existing "Post Vacancy" pattern already in the same module. Blocks a duplicate submission (same email, same vacancy) while correctly allowing the same candidate to apply to a different open role. | Phase 4 | **VERIFIED live**: submitted a new application against a real open vacancy, visible immediately in the Applications tab; a duplicate submission (same email + vacancy) correctly rejected; the same email against a different vacancy correctly succeeded. |
| KOM-008 | H-02 (severity corrected from High to Critical — see reason) | Entire Training module fatally broken in 3 separate places, plus 2 silent display bugs — not just 1 column mismatch as originally reported | Training | `modules/training/{enrol,index,save}.php` | Originally reported (Phase 0) as a single mismatch: `enrol.php` writes `program_id`, `index.php` reads `training_id`. Investigating it directly for Phase 4 found the module fatally broken in three separate places: (1) `enrol.php`'s duplicate-check AND insert both referenced nonexistent columns (`program_id`, `created_by`) — every enrolment attempt crashed; (2) the Attendance tab's own list query did `ORDER BY ta.created_at DESC` on a column that doesn't exist — even viewing existing enrolments crashed, independent of (1); (3) `save.php`'s Add Program insert referenced `training_type`/`trainer_name`/`venue`, none of which exist (real columns: `provider`/`location`, no type column at all) — creating a program crashed too. On top of the 3 crashes, the Attendance and Programs list *displays* silently showed blank/wrong values for further nonexistent columns (`status`, `score`, `training_type`, `trainer_name`) since PHP treats a missing array key as null rather than erroring. No part of this module — create a program, enrol someone, or view who's enrolled — worked at all. Severity corrected to Critical. | **FIXED — Phase 4.** All three crash sites corrected to real column names; both list displays corrected to show real data (Attended Yes/No + certificate status, replacing the fictional Status/Score columns; Provider/Location, replacing Type/Trainer); the misleading "Type" dropdown removed from the Add Program form since it had no destination column. New `attendance_update.php` + "Mark Attended" action — there was previously no way to ever update `attended` after enrolment, not even a broken one. | Phase 4 | **VERIFIED live**: created a program, enrolled an employee, viewed the (previously crashing) Attendance tab, and marked the enrolment attended — all four steps working end-to-end for the first time. See `Workflows/08-training-workflow-report.md` §1. |
| KOM-093 | Phase 4 — new | Stored XSS in the notification bell — any employee can execute script in an HR Manager's or Super Admin's session | Notifications / Employee Portal | `includes/footer.php`, `employee-portal/hub.php` | The notification dropdown (used by every logged-in user, all roles) rendered `title`/`message` via a JS template literal assigned directly to `.innerHTML`, with no escaping anywhere in the chain — not server-side (`api/notifications.php` returns raw `SELECT *` as JSON) and not client-side. `employee-portal/hub.php`'s self-service request form passes the employee's own raw, free-text `subject` straight into a notification sent to every `hr_manager` and `super_admin`. Live-verified: submitted a hub request with `subject=<script>alert(1)</script>` as a plain employee — the payload was stored verbatim and returned verbatim by the notifications API; only escaping at DOM-insertion time would neutralize it. This is a genuine privilege-escalation chain: the lowest-privilege authenticated role (a portal employee, no special permission needed) can execute arbitrary script in the browser session of the highest-privilege live roles, with access to their live CSRF token and full session — worse in reach than the other stored-XSS finding in this register (KOM-022, which requires template create/edit rights first). | **FIXED — Phase 4.** Added an `escapeHtml()` helper (`textContent`/`innerHTML` round-trip, the standard DOM-based-XSS-safe idiom) in `footer.php`'s notification renderer and applied it to `title`, `message`, and the resolved `link` before any of them touch `.innerHTML` — a single fix at the shared rendering chokepoint that every current and future notification source flows through, not a patch to the one exploitable call site. | Phase 4 | **VERIFIED live**: confirmed the notifications API still returns the raw, dangerous payload (escaping was never happening server-side and correctly still doesn't — output encoding belongs at the point of DOM insertion); confirmed by code review that `escapeHtml()` is applied to every interpolated field in the renderer. Full click-through browser verification (opening the actual dropdown as an admin and confirming no script fires) was not performed in this environment — this fix uses a standard, well-established escaping idiom and was verified by direct code review plus confirming the unescaped payload reaches the client, but a live browser click-through is recommended before Phase 5 sign-off. |

---

## HIGH (28)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase | Verification Required |
|---|---|---|---|---|---|---|---|---|
| KOM-007 | H-01 | Leave application crashes after it's already been saved | Leave | `modules/leave/apply.php` | `notifyRole()` called with an array where a single string role is required (also missing an argument vs. current signature) — throws after the `INSERT` has committed. | **FIXED — Phase 4.** Correctly out of scope for Phase 2 (authentication/session, not business workflow); addressed in Phase 4's Leave Management workflow group. `notifyRole()` now called once per role with correctly ordered arguments; also caught and fixed the same class of mistake (`notifications.type` ENUM misuse) in Phase 4's own earlier-added notification calls before they went live. | Phase 4 | **VERIFIED live**: leave submission now returns a clean redirect with no fatal error; `hr_manager`/`super_admin` both receive a correctly-populated notification. See `Workflows/03-leave-management-workflow-report.md` §3. |
| KOM-009 | H-03 | Leave approve/reject buttons non-functional | Leave | `modules/leave/{view,approve}.php` | `view.php` POSTs hidden fields; `approve.php` only reads `$_GET` — button clicks silently no-op. | Open (reconfirmed) | Phase 2 | Functional test: click Approve from the detail page, confirm status changes |
| KOM-010 | H-04 | Timesheet/overtime approval checks view permission, not approve | Timesheets | `modules/timesheets/{approve,overtime}.php` | `requirePermission('timesheets.approve')` called with no action argument, defaults to `'view'`. | **FIXED — Phase 1.** `approve.php`'s `approve` branch (the one remaining gap after the earlier partial fix) now explicitly checks `requirePermission('timesheets.approve','approve')` before mutating; `overtime.php`'s approve/reject branch now checks `requirePermission('timesheets.approve_ot','approve')`. | Phase 1 | Code-verified (no role in the current seed data has can_view=1/can_approve=0 for these exact slugs, so the specific bypass can't be independently demonstrated live with today's data — the code path is confirmed correct by review and by the page-level/action-level gates now being distinct calls). |
| KOM-011 | H-05 | Executive Analytics exposes company-wide payroll totals without salary-data permission check | Reports | `modules/reports/executive.php` | Requires only `reports.view`; never calls `canViewSalaryData()`/`maskSalary()` before rendering aggregate payroll totals/CSV export. | **FIXED — Phase 1.** Both the "YTD Payroll" KPI card and the "Payroll Summary" card now check `canViewSalaryData()` and render `maskSalary()`'s blurred placeholder when false; headcount-only figures (employees on payroll) remain unmasked, consistent with the dashboard's existing pattern. | Phase 1 | **VERIFIED live**: super_admin and payroll_officer (both hold `payroll.view`) see real figures with zero `filter:blur` occurrences on the page. No currently-seeded role has `reports.view` without `payroll.view`, so the masked path itself is code-verified rather than independently live-demonstrated with today's data — see Regression Test Report. |
| KOM-012 | H-07 | Consultant portal login has no session-ID regeneration (session fixation) | Consultant Portal | `consultant-portal/login.php` | No `session_regenerate_id()` call anywhere in the file, confirmed by full-text search. | **FIXED — Phase 2.** New shared `regenerateSessionOnLogin('cp_')` called immediately on successful auth, before any session data is written. | Phase 2 | **VERIFIED live** — session ID confirmed to change between pre-login and post-login requests. |
| KOM-013 | H-08 | No CSRF protection on consultant self-service actions | Consultant Portal | `consultant-portal/{kiosk,scope}.php` | Clock in/out/break and scope-notes save have no CSRF token on form or handler. | **FIXED — Phase 2.** All 5 kiosk forms and the scope note-save form now carry a CSRF token, verified server-side before any database write. | Phase 2 | **VERIFIED live** — a clock-in POST without a token was rejected with zero database change; the identical request with a valid token succeeded and wrote the expected row. |
| KOM-014 | H-09 | Payroll deduction/savings delete bypasses the permission matrix's delete restriction | Payroll | `modules/payroll/{deductions,savings}.php` | `requirePermission()` called with no action (defaults to view); delete branches inherit that gate despite the matrix setting `can_delete=0` for payroll roles. | **FIXED — Phase 1.** Every action branch (create/toggle/update_balance/delete) in both files now calls `requirePermission()` with its own correct action; delete branches additionally now call `auditLog()` (previously silent). | Phase 1 | **VERIFIED live**: `payroll_officer` (can_view/create/edit=1, can_delete=0 per matrix) attempted a delete POST and was redirected with `error=access_denied`, no row removed. See Regression Test Report. |
| KOM-015 | H-10 | User role assignment has no server-side allow-list — privilege escalation | Users | `modules/users/index.php` | `$_POST['role']` inserted after only a non-empty check; an HR Manager can POST `role=super_admin`. | **FIXED — Phase 1.** New `VALID_USER_ROLES`/`assignableRoles()`/`isValidAssignableRole()` in `config/functions.php`: only `super_admin` may grant `super_admin`; every role-assigning action (`add_user`, `toggle_user`, `reset_password`) now validates server-side and re-checks the *target* user's role isn't outside the actor's granting authority. `create`/`edit` actions also now individually permission-checked (previously only the page-level `view` gate applied). | Phase 1 | **VERIFIED live**: `hr_manager` POSTed `role=super_admin` directly to `add_user`; no row was created in `users` with that role — confirmed by direct DB query post-attempt. See Regression Test Report. |
| KOM-016 | H-11 | Published payslips editable with no audit trail | Payroll | `modules/payroll/payslips.php` | Update branch has no status guard (can edit `finalized`/`sent` payslips) and never calls `auditLog()`. | Open (reconfirmed) | Phase 2 | Functional test: edit a published payslip, confirm rejection or that an audit entry is recorded |
| KOM-017 | H-12 | Employee portal login has no session-ID regeneration (session fixation) | Employee Portal | `employee-portal/login.php` | Neither the permanent nor temp-employee branch calls `session_regenerate_id()`. | **FIXED — Phase 2.** Both the permanent-employee and temp-employee success branches now call `regenerateSessionOnLogin('ep_')` before writing session data. | Phase 2 | **VERIFIED live** for both branches — session ID confirmed to change on login in both the permanent-employee and temp-employee test runs. |
| KOM-018 | NH-01 | Dashboard leaks raw audit-log data to every logged-in user regardless of role | Dashboard | `dashboard.php` | Page gated by `requireLogin()` only; "Recent Activity" widget runs `SELECT * FROM audit_logs LIMIT 8` and renders it unconditionally — no `audit.view`/`activity_log` permission check on this specific widget. | **FIXED — Phase 1.** The widget's query is now conditional on `canView('audit.view') \|\| canView('activity_log.view')`; roles without either permission get an empty array and the page's existing empty-state ("No activity recorded yet.") renders instead. | Phase 1 | **VERIFIED live**: `payroll_officer` (holds neither permission) sees the empty-state text; `super_admin` sees populated data. See Regression Test Report. |
| KOM-019 | NH-02 | Activity Log module bypasses the permission system entirely | Activity Log | `modules/activity_log/{index,user,download}.php`, `includes/header.php` | Hardcodes `$_SESSION['user_role'] !== 'super_admin'` instead of `requirePermission()`; not controllable via the Roles & Permissions UI at all. | **FIXED — Phase 1.** New seeded permission `activity_log.view` (`database/phase10_authorization_framework.sql`), granted to `super_admin` only — matching prior behavior exactly (mechanism change, not an access-policy change). `index.php`/`user.php` now call `requirePermission('activity_log.view','view')`; `download.php` (a raw CSV response, not an HTML page) uses `hasPermission(...,'export')` with a manual 403 to preserve its non-redirect response shape. `includes/header.php` nav link and the Administration-section visibility check both converted to `canView('activity_log.view')`. | Phase 1 | **VERIFIED live**: super_admin → 200; hr_manager, hr_officer, payroll_officer → 302 (all three previously blocked by the hardcoded check too — access unchanged, only the mechanism is now DB-driven). See Regression Test Report. |
| KOM-020 | NH-03 | Reflected XSS in temp_employees export status banner | Temp Employees | `modules/temp_employees/index.php` | `$_GET['status']` echoed unescaped in the PDF export banner, while the adjacent search-term display correctly escapes. | **FIXED — Phase 4.** Correctly out of scope for Phase 2 (authentication/session, not business workflow); discovered still open and fixed while reviewing the Temp Employee module in Phase 4. `htmlspecialchars()` now wraps the value before output, matching the pattern already used for the adjacent search-term display. | Phase 4 | **VERIFIED live**: `?export=pdf&status=<script>alert(1)</script>` now renders the payload as inert, HTML-entity-encoded text (`&lt;script&gt;...`) with zero occurrences of the raw, executable tag in the response. |
| KOM-021 | NH-04 | Any documents.view holder can view/print any employee's generated document by guessing an ID (IDOR) | Documents | `modules/documents/view_generated.php` | Fetches any `generated_documents` row by `id` with only the blanket `documents.view` check — no per-employee scoping, no audit entry on view. | **FIXED — Phase 1.** New `canAccessGeneratedDocument()` in `config/functions.php`: `approved`/`issued` documents remain visible to any `documents.view` holder (the module's finished output, HR-tier roles legitimately need org-wide visibility); `draft`/`pending_approval` documents are now restricted to their generator (`generated_by`) or a `documents.verify` holder. Every successful view is now audited via `auditLog('documents','view_document',...)` (previously only generate/approve/issue were logged, not view). | Phase 1 | Code-verified. Every currently-seeded role holding `documents.view` (hr_manager, hr_officer) also holds `documents.verify` approve rights, so the owner-vs-non-owner draft restriction can't be independently demonstrated live with today's role data — the code path and its logic were verified by review and by confirming the page still loads correctly end-to-end. |
| KOM-022 | NH-05 | Document template body is unsanitized — stored XSS via template authoring | Documents | `modules/documents/templates.php`, `config/DocumentEngine.php` | `render()` only ever escaped `{{placeholder}}` *values*, never the surrounding markup — a `hr_officer`/`hr_manager` (the two roles holding `documents.upload`) authoring a template body as raw HTML in a plain `<textarea>` could embed `<script>` or an `onerror=` handler that executes for every future viewer of any document generated from it, including more-privileged roles (a `documents.verify` approver, or `super_admin`) who never had a chance to review the raw source first. Correctly out of scope for Phase 1 (not an authorization-consistency finding); addressed here as part of Phase 4's Documents generation lifecycle review. | **FIXED — Phase 4.** New `DocumentEngine::sanitizeTemplateHtml()` (DOMDocument-based, no external dependency) applied at save time in `templates.php` — denies dangerous tags outright (`script`, `iframe`, `object`, `embed`, `form`, etc.) and strips event-handler attributes plus `javascript:`/`vbscript:`/`data:text/html` URL schemes from every remaining element, while leaving legitimate rich HTML (tables, divs, inline styles, images, and the `{{variable}}` placeholders themselves) untouched. A libxml quirk that percent-encodes `{{...}}` specifically inside `href`/`src`-type attributes (which would have silently broken any template linking to e.g. `{{company.website}}`) was found during testing and fixed with a protect/restore token swap around the sanitization pass. | Phase 4 | **VERIFIED live**: saved a real test template via the actual `hr_manager` UI flow with a `<script>`, an `onerror=` handler, and a `javascript:` href all present — all three neutralized in the stored `body_html`, while a legitimate `{{company.website}}` placeholder inside an `href` survived intact. Generated an actual document from the sanitized template for a real employee and confirmed the full pipeline — `{{employee.full_name}}` correctly substituted, malicious markup still absent — end to end. Also scanned all 47 existing live templates for known-dangerous markup: zero matches, so no retroactive re-sanitization of existing data was needed. |
| KOM-023 | Phase 0 — new | `hrofficer`/`hr_officer` role-name typo silently denies two modules to a legitimate role | Permissions / Temp Employees / Consultants | `database/phase8_temp_employees.sql`, `database/phase9_consultants.sql` | Both files seed permissions to role string `'hrofficer'` (no underscore); the canonical ENUM value everywhere else is `'hr_officer'`. Real `hr_officer` users get zero `role_permissions` rows for `temp_employees.*`/`consultants.*` — a functional access-denial bug affecting every HR Officer, not a security exposure. | **FIXED — Phase 1 (live DB), FIXED AT SOURCE — Phase 3.** Phase 1's `phase10_authorization_framework.sql` UPDATE patched the *running* database only; the typo still lived on in `phase8_temp_employees.sql`/`phase9_consultants.sql` themselves, so a fresh install would have reintroduced the exact same bug. Phase 3 corrected both seed files directly to `'hr_officer'` — a fresh install and the live database are now identical on this point. | Phase 1 (live), Phase 3 (source) | **VERIFIED live** (Phase 1) plus **VERIFIED via clean-install test** (Phase 3): zero `hrofficer` typo rows and correct `hr_officer` → `temp_employees.*` grants (4) confirmed on a database built only from tracked source files. |
| KOM-024 | Phase 0 — new | Fresh installation is missing all phase 1–9 tables, permissions, and templates | Deployment / Database | `database/install.php` | Installer executes `schema.sql` only; on-screen instructions mention `migration_v2.sql` but never phase1/5/6/7/8/9 — a fresh install following only the installer's own guidance ends up without the permission matrix, branding tables, 47 document templates, or the temp-employee/consultant modules' schema. | **FIXED — Phase 3.** `database/install.php` rewritten around an explicit `INSTALL_SEQUENCE` constant that runs `schema.sql` → baseline admin seed → `doc_categories` seed → `phase1_permissions.sql` → `phase5_branding_theme.sql` → `phase6_templates.sql` → `phase8_temp_employees.sql` → `phase9_consultants.sql` → `phase10_authorization_framework.sql` in one pass, stopping and reporting clearly on the first failed step. Optional demo data via a checkbox, never on by default. | Phase 3 | **VERIFIED live**: `database/verify_clean_install.php` (CLI, re-runnable) builds a genuinely empty database through this exact sequence and confirms 60/60 tables, all previously-missing columns, 96 permissions, 47 templates, 10 categories, a forced-password-change super_admin, zero `hrofficer` typo rows, and correct `hr_officer` temp_employees grants (26/26 checks) — then a full HTTP smoke test (login → forced password change → all 12 critical modules 200 OK) against the same freshly-built database. See `Testing/12-phase3-clean-install-test-report.md`. |
| KOM-068 | Phase 3 — new | `doc_categories` seed data existed nowhere in the repository despite being a hard install dependency | Database / Documents | `database/phase6_templates.sql`, `database/seeds/002_doc_categories.sql` (new) | `phase6_templates.sql` resolves each template's category via `SET @cat_x = (SELECT id FROM doc_categories WHERE slug='...')`, and `doc_templates.category_id` is `NOT NULL` — a fresh install had no file that created the 10 category rows this depends on, so template loading would fail outright on an empty database (undiscovered until Phase 3's clean-install testing, since the live database already had this data from an untracked manual step). | **FIXED — Phase 3.** New `database/seeds/002_doc_categories.sql`, with the exact 10 categories verified byte-for-byte against the live database, added to the install sequence immediately before `phase6_templates.sql`. | Phase 3 | **VERIFIED live**: clean-install test confirms 10/10 categories seeded and all 47 templates load with valid `category_id` values on a database built only from tracked files. |
| KOM-069 | Phase 3 — new | Employee portal policy-agreement form has no CSRF token | Employee Portal | `employee-portal/policy.php` | The state-changing POST that records `portal_policy_agreed`/`portal_policy_agreed_at` against the employee's record had no CSRF token at all — predates and was missed by Phase 2's portal-wide CSRF standardization pass, discovered during Phase 3 database-layer work. | **FIXED — Phase 3.** `generateCsrfToken()`/`verifyCsrfToken()` added, matching the pattern used by every other portal mutation; invalid/missing token redirects back to the policy page without recording agreement. | Phase 3 | **VERIFIED live**: agreement POST without a token does not set `ep_policy_agreed`/does not update the database row; POST with a valid token extracted from a real page load succeeds. See `Testing/14-phase3-regression-test-report.md`. |
| KOM-072 | Phase 4 — new | Promotion, transfer, and termination bypass the approval engine entirely | Employees / Approvals | `modules/employees/{status,edit}.php`, `config/ApprovalEngine.php` | `approval_workflows.workflow_type` and `ApprovalEngine::workflowConfig()` both explicitly define `promotion`/`transfer`/`termination` as approval-gated workflow types (each with an `hr_manager` review stage) — but no code anywhere called `ApprovalEngine::create()` for any of the three. Only `leave` of the engine's 8 defined types was ever actually used. A single actor with `employees.status:edit` could terminate/promote/transfer any employee instantly, alone, with no second-person review. | **FIXED — Phase 4, per explicit user direction (wire all three).** `status.php` now creates a `termination` workflow instead of applying the status change immediately; `edit.php` detects department/supervisor changes as `transfer` and position/salary changes as `promotion`, holding those specific fields at their current value until approved. `ApprovalEngine::updateReference()` extended to apply the proposed change from the workflow's `notes` JSON on approval only — rejection leaves the employee record untouched. | Phase 4 | **VERIFIED live**: termination submitted by one actor, approved by a different `hr_manager` user (honoring the engine's separation-of-duties check) — status only changed after approval; a second termination request was rejected and confirmed to leave the employee untouched; a department transfer and a position/salary promotion were each submitted, held pending, then correctly applied on approval. See `Workflows/01-employee-management-workflow-report.md` §3. |
| KOM-074 | Phase 4 — new | Reactivating an employee's status doesn't restore their disabled user account | Employees / Users | `modules/employees/status.php` | Exiting statuses (`resigned`/`terminated`/`deceased`/`archived`) correctly disabled the linked `users` row (`is_active=0`), but the reverse — returning to `active`/`probation`, e.g. a rehire handled via reactivation — never re-enabled it. A legitimately reactivated employee's account stayed silently, permanently locked out. | **FIXED — Phase 4.** Added the missing re-enable branch. | Phase 4 | **VERIFIED live**: terminate→reactivate cycle against a disposable test employee confirms `is_active` now correctly flips 1→0→1. |
| KOM-079 | Phase 4 — new | Adding a duplicate department name crashed the entire Settings page | Departments | `modules/settings/index.php` | `departments.name` has a DB-level `UNIQUE KEY` (unconditional — also blocks reusing a disabled department's name), but the Add handler never checked for a collision before the `INSERT`. Live-verified: submitting an existing department name threw an uncaught `PDOException`, crashing the page. | **FIXED — Phase 4.** Application-level duplicate-name check added before the `INSERT`, distinguishing an active vs. disabled existing department. | Phase 4 | **VERIFIED live**: duplicate submission now shows a clean inline error, no crash, no duplicate row; a genuinely new name still succeeds. |
| KOM-082 | Phase 4 — new | `approval_workflows` never reflected the real leave approval/rejection decision | Leave / Approvals | `modules/leave/approve.php` | `apply.php` creates a real `ApprovalEngine` workflow, but the page that actually decides the outcome (`approve.php`) updates `leave_applications` directly and never calls `ApprovalEngine::act()`. Live-verified: approved a leave application through the normal Leave module UI — `leave_applications.status` correctly became `approved`, but the linked `approval_workflows` row stayed `pending` permanently, so the Approvals module would show it as still awaiting action forever. | **FIXED — Phase 4.** `approve.php` now also resolves the matching `approval_workflows` row and every one of its `approval_stages` (not just the current stage, since this page finalizes the decision outright). | Phase 4 | **VERIFIED live**: approving a leave application now correctly flips both stages and the parent workflow to `approved` in one action. |
| KOM-085 | Phase 4 — new | Payroll deductions and savings are never reflected in payslip totals | Payroll | `modules/payroll/{payslips,deductions,savings}.php` | `payslips.php` computes `total_deductions`/`net_salary` from only `tax_amount + uif_employee + other_deductions`, entered directly on the payslip form — it never reads `payroll_deductions` (loans, union dues, garnishees, pension, medical aid) or `employee_savings`, and neither of those modules ever writes back to the payslip. A properly-designed `payslip_items` line-item table exists in the schema but is referenced by zero code. Live data check: 29 employees currently have `payroll_deductions` rows, 13 have `employee_savings` rows — not hypothetical, real tracked amounts that do not affect payslip calculations today. | **ACCEPTED AS DESIGNED — user decision, 2026-07-12.** Deductions/savings remain record-keeping only; payslip entry stays an independent manual step by HR. No code change made — this is the confirmed intended behavior, not a defect. | Phase 4 (accepted) | Decision recorded; no further action required unless revisited. |
| KOM-088 | Phase 4 — new | No employee-conversion step exists, despite the schema being built for it | Recruitment | `modules/recruitment/*`, `modules/employees/add.php` | `recruitment_applications.converted_to_employee_id` exists in the schema — clear evidence of intended design — but no code anywhere reads or writes it. The pipeline stops at `selected`/`rejected`/`withdrawn`; HR must manually re-type a selected candidate's details into Add Employee with zero connection back to the original application. | **FIXED — Phase 5, Stage 5.7 (user decision: guided conversion, not a separate system).** A "Convert to Employee" action on a `selected` application (requiring both `recruitment.review` and `employees.create`) pre-fills the existing Add Employee form with the applicant's name/email/phone via `?from_application=<id>`; on save, `converted_to_employee_id` is set (re-validated at POST time against the same convertible-state condition, so a stale/duplicate submission can't double-convert), and an audit entry links the two records. Once converted, the UI shows "View Employee" in place of the action, preventing re-conversion through the interface. | Phase 5 | **VERIFIED live**: pre-fill confirmed on GET; full conversion completed and confirmed via `converted_to_employee_id` and the audit log; re-request after conversion correctly falls through to a blank form rather than re-linking; baseline Add Employee page (no `from_application`) confirmed unaffected. See `Phase5/08-recruitment-conversion-report.md`. |
| KOM-092 | Phase 4 — new | `auditLog()` called with the wrong argument order across the entire Temp Employee module — corrupts `record_id`/`old_value` for every create/edit/delete | Temp Employees | `modules/temp_employees/{add,edit,delete}.php` | All three files called `auditLog('temp_employees', '<action>', $_SESSION['user_id'], 'temp_employees', $id, "...")` — but the real signature is `auditLog(module, action, recordId, oldValue, newValue, reason)`. The acting admin's own user ID landed in `record_id` instead of the temp employee's ID, the literal string `'temp_employees'` landed in `old_value`, and the employee's real ID was silently discarded into `new_value`. The human-readable `reason` text was unaffected, but every structured audit-trail query by `record_id` for this module was returning the wrong records or none at all. Live-verified: deleting a test temp employee (id 9) recorded `record_id=1` (the logged-in admin's user ID) instead of `9`. | **FIXED — Phase 4.** All three calls corrected to `auditLog('temp_employees', '<action>', $id, null, null, "...")`, matching the signature already used correctly by the employees and consultants modules. | Phase 4 | **VERIFIED live**: created, edited, then deleted a disposable test temp employee — all three resulting `audit_logs` rows now show the correct `record_id` (the temp employee's own ID) matching the actual affected row. |
| KOM-095 | Phase 4 — new | `ApprovalEngine::act()` never notified anyone of an approve/reject decision — HR staff had no way to learn the outcome of a termination/transfer/promotion request except by manually re-checking the Approvals page | Approvals | `config/ApprovalEngine.php` | `leave/approve.php` bypasses the engine entirely and has always notified its applicant directly on decision, but every workflow that actually flows through the shared `act()` method (in current practice: `termination`, `transfer`, `promotion` — the only three of the engine's 8 defined types ever actually created) resolved the workflow and applied the real employee change via `updateReference()` with zero notification to anyone. The HR staff member who submitted a termination/transfer/promotion request had no in-app signal that a decision had even been made. | **FIXED — Phase 4.** New `notifyInitiator()` private method, called from both the reject and final-stage-approved branches of `act()`, notifies `approval_workflows.initiated_by` (the HR staff member who submitted the request) with the outcome and any reviewer comments — generalizing the same idea leave's own `approve.php` already used, but at the shared engine level so every current and future workflow type gets it for free. Deliberately does **not** notify the employee the workflow is *about* (e.g., the person being terminated) — that is a human-conversation matter, not an automated in-app popup, and was kept out of scope as a separate, more sensitive design question. | Phase 4 | **VERIFIED live**: submitted a termination request as one admin, approved it as a different `hr_manager` user (honoring the engine's separation-of-duties check), confirmed the initiating admin received a correctly-typed (`success`) notification with the reviewer's comments; workflow and employee status both correctly updated. |
| KOM-098 | Phase 4 — new | `attendance.is_absent` is dead code everywhere it's read — every "Absent" figure across the Dashboard and Reports has been silently, permanently wrong | Dashboard / Attendance / Reports | `dashboard.php`, `modules/attendance/index.php`, `modules/reports/{index,executive,timesheets}.php` | `is_absent` defaults to `0` and is never `INSERT`ed or `UPDATE`d by any code path anywhere in the codebase — a row only ever gets created when an employee actually clocks in via the kiosk, so a true absence (no clock-in at all) never produces a row for `is_absent` to be set on in the first place. Every `WHERE is_absent=1`/`SUM(is_absent)` across the app is therefore structurally guaranteed to return 0, always. Live-verified: with 13 active employees and 0 clocked in, the Dashboard's "Absent Today" showed 0 (its own sub-label already said "Not clocked in," confirming this was the intended, just-miswired, calculation). | **FIXED — Phase 4 (daily instances) + Phase 5, Stage 5.3 (monthly/period instances, completing the deferral).** Phase 4 fixed the two daily instances (Dashboard's "Absent Today"/7-day trend; the Attendance module's single-day summary). Phase 5 built the working-day/holiday calendar infrastructure this finding's monthly instances needed (`work_calendar_settings`/`work_calendar_holidays`, `isWorkingDay()`/`countWorkingDays()`/`getWorkingDaysBetween()`/`getNextWorkingDay()`) and used it to correctly compute `modules/reports/index.php`'s per-employee `absent_days` (now `employees LEFT JOIN attendance` so a never-clocked-in employee still appears, instead of being silently excluded), `modules/reports/executive.php`'s Attendance Rate KPI (now against true expected headcount×working-days, not just attendance-row count), and `modules/reports/timesheets.php`'s per-employee absent count. Also improved the daily figures further: Absent Today/the 7-day trend/the Attendance module's day view now correctly show 0 on a non-working day rather than the full headcount. See `Phase5/04-working-day-calendar-report.md`. | Phase 4 (daily) + Phase 5 Stage 5.3 (monthly/period) | **VERIFIED live**: 17/17 calendar unit tests pass (weekends, cross-month, leap year, holiday exclusion, recurring-annual, inactive-holiday non-blocking). Dashboard Attendance Rate moved from 0%→1% correctly on a disposable test clock-in; Absent Today 13→12→13 across insert/cleanup. |

---

## MEDIUM (31)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase | Verification Required |
|---|---|---|---|---|---|---|---|---|
| KOM-025 | M-01 | Employee salary field always blank on edit | Employees | `modules/employees/edit.php` | Reads `$emp['salary']`; canonical/actual column is `basic_salary`. | Open — not re-verified in Audit II | Phase 3 | Functional test: open edit form for an employee with a salary set, confirm field is pre-populated |
| KOM-026 | M-03 | Notification "mark read" endpoints are CSRF-able via plain GET | Notifications API | `api/notifications.php`, `includes/footer.php` | `mark_read`/`mark_all_read` mutate state on GET with only a login check, no token. | **FIXED — Phase 2.** Both actions now require POST + a verified CSRF token, rejecting with HTTP 403 otherwise; `includes/footer.php`'s fetch calls updated to send POST with a token exposed via a new `window.CSRF_TOKEN` JS global. `list`/`count` remain GET (pure reads, no state change). | Phase 2 | **VERIFIED live**: GET rejected, POST without token rejected, POST with valid token (extracted from a real page load) succeeded. |
| KOM-027 | M-04 | Employee portal hub request submission has no CSRF token | Employee Portal | `employee-portal/hub.php` | Unlike the HR-facing counterpart, which does verify CSRF. | **FIXED — Phase 2.** Form now carries a CSRF token, verified server-side before the INSERT. | Phase 2 | **VERIFIED live**: submission without a token rejected with zero rows inserted; submission with a valid token succeeded. |
| KOM-028 | M-05 | Reports Hub "Export CSV" link is dead | Reports | `modules/reports/index.php` | Nothing reads `$_GET['export']`, unlike employees/timesheets/executive reports where it works. | Open | Phase 3 | Functional test: click Export CSV, confirm a file downloads |
| KOM-029 | M-06 | Temp-employee portal has weaker session lifecycle than the main portal | Employee Portal (temp) | `employee-portal/temp_portal.php` | Doesn't include the shared `_session.php`; no 30-min ID rotation, no true inactivity timeout, relies only on the 8h absolute cookie. | **FIXED — Phase 2.** `temp_portal.php` now includes `_session.php` directly (same 'ep_' prefix, same session store as the permanent-employee portal) via the new shared `bootstrapSession()`/`epRequireTempLogin()` — full ID rotation and idle timeout now apply identically to temp employees. | Phase 2 | **VERIFIED live**: temp employee login, portal access, and logout all confirmed working through the shared framework end-to-end. |
| KOM-030 | M-07 | Payroll run publish/finalize has a check-then-act race condition | Payroll | `modules/payroll/{run_save,run_publish,run_finalize}.php` | `SELECT` status check followed by `UPDATE`/`INSERT`, no transaction/row lock — concurrent submissions can both pass the check, risking duplicate payslip emails. Also found the same pattern in `run_save.php` (create), where the DB's own `UNIQUE KEY` backstop meant the losing request hit an uncaught `PDOException` rather than an actual duplicate row. | **FIXED — Phase 4.** All three actions now make the status transition itself the atomic guard (`UPDATE ... WHERE id=? AND status='<expected>'`, checked via `rowCount()`); `run_save.php` additionally catches the duplicate-key case explicitly. | Phase 4 | **VERIFIED live with a genuine concurrency test**: fired 5 simultaneous publish requests at the same run — exactly 1 `audit_logs` publish entry resulted, confirming only one request proceeded past the guard. See `Workflows/05-payroll-workflow-report.md` §1. |
| KOM-031 | M-08 | SMTP password exposed in page HTML source | Settings | `modules/settings/email.php` | Second "Payslip Notifications" form re-emits `smtp_pass` as a hidden input's cleartext `value` attribute (primary form now correctly uses a placeholder). | **Partially changed, still exploitable** — primary form fixed, second form still leaks | Phase 3 | Manual check: View Source on the settings page, confirm no cleartext password anywhere post-fix |
| KOM-032 | M-09 | One permission grants CRUD over four unrelated branding asset types | Settings / Branding | `modules/settings/branding.php` | Gates letterheads/signatures/stamps/watermarks (incl. create/edit/delete) behind a single `branding.letterheads` check, despite 4 separate permission slugs existing in seed data. | **FIXED — Phase 1** (pulled forward from Phase 3 — directly named in the Phase 1 charter's findings list). All 12 action branches (save/toggle/delete × 4 asset types) now call `requirePermission()` with their own type-specific slug (`branding.letterheads`/`.signatures`/`.stamps`/`.watermarks`) and action; page-level gate now requires view rights on *any* of the four rather than hardcoding `letterheads`. | Phase 1 | **VERIFIED**: syntax-checked and smoke-tested live (200 OK, no errors); not independently demonstrable with today's seed data since `hr_manager`/`super_admin` currently hold identical full CRUD on all four types — the fix closes a latent/structural gap rather than a currently-exploitable one. See Regression Test Report and Permission Consistency Report. |
| KOM-033 | NM-01 | Activity Log CSV export doesn't neutralize spreadsheet formula injection | Activity Log | `modules/activity_log/download.php` | Escapes double-quotes but not leading `=`,`+`,`-`,`@` in free-text exported fields (reason, old_value, new_value). | Open | Phase 3 | Security test: export a record containing a formula-injection payload, confirm neutralization post-fix |
| KOM-034 | NM-02 | Activity Log per-user export ignores filters and loads unbounded data | Activity Log | `modules/activity_log/download.php` | Per-user export path ignores date-range filters respected elsewhere in the same file; uses `fetchAll()` instead of the streaming pattern used by category exports. | Open | Phase 3 | Functional test: export a long-tenured user's history with a date filter applied, confirm the filter is respected post-fix |
| KOM-035 | NM-03 | Temp Employees audit trail logs the wrong record ID | Temp Employees | `modules/temp_employees/{add,edit,delete}.php` | `auditLog()` called with the acting admin's user ID in the record-ID argument, instead of the affected temp employee's ID. | **RESOLVED — Phase 5, superseded by KOM-092.** This is the identical defect, same files, same root cause as KOM-092, which was independently discovered and fixed in Phase 4 Workflow Group 10 without being cross-referenced to this row. Found and corrected during Phase 5's open-findings audit (see `Phase5/01-phase5-open-findings-scope.md` §0). No code change needed here — KOM-092's fix already resolved this. | Phase 5 | See KOM-092. |
| KOM-036 | H-06 + NM-04 | Document generation save action does not re-check create-level permission | Documents | `modules/documents/generate.php` | Whole page gated by `documents.view`; the `save` action (which persists a new document containing PII) never separately checks a create/upload-level permission, unlike `templates.php`. Not exploitable under current seeded roles (view+create are bundled for every role holding either), but a genuine code-level gap. | **FIXED — Phase 1** (pulled forward — directly named in the Phase 1 charter's findings list). The `save` branch now calls `requirePermission('documents.upload','create')` before persisting, matching the pattern `templates.php` already used. | Phase 1 | Code-verified + smoke-tested live (200 OK, no errors). Not independently live-demonstrable today since every role with `documents.view` also holds `documents.upload` create — same reasoning as when this was first reported; the fix removes the latent gap for when that changes. |
| KOM-037 | NM-05 | Two menu items, two authorization models, one underlying audit table | Navigation | `includes/header.php`, `modules/audit/index.php`, `modules/activity_log/*` | "Audit Logs" (permission-gated via `audit.view`) and "Activity Logs" (hardcoded super-admin-only) sit adjacent in the sidebar, both reading `audit_logs`. | Open | Phase 3 | Product decision + code review: confirm a single, consistently-gated entry point post-fix |
| KOM-038 | NM-06 | SVG accepted for letterhead uploads | Settings / Branding | `modules/settings/branding.php` | `image/svg+xml` allowed for letterheads only; SVG can carry embedded script; server-side MIME sniffing will pass a well-formed malicious SVG. Not currently exploitable (rendered only via `<img src>`), but stacks with KOM-006/KOM-039. | Open | Phase 3 | Code review: confirm SVG is excluded or sanitized post-fix |
| KOM-039 | NM-07 | Uploaded branding file extension trusted from client filename | Settings / Branding | `config/functions.php` (`uploadFile()`) | Extension saved to disk comes from the client-supplied original filename, not cross-checked against the MIME type detected by `finfo`. | Open | Phase 3 | Security test: upload a polyglot file with a mismatched extension, confirm rejection post-fix |
| KOM-040 | Phase 0 — new (consolidates M-02) | Hardcoded role-list authorization checks bypass the DB-driven permission system (13 occurrences, 12 files) | System-wide | `modules/timesheets/corrections.php:17`, `modules/employees/view.php:769`, `modules/assets/index.php:85`, `modules/documents/index.php:137`, `modules/leave/{view.php:35,apply.php:17,index.php:147}`, `modules/archive/monthly.php:155`, `modules/temp_employees/{view.php:278,add.php:263,edit.php:273}`, `modules/activity_log/{index.php:26,download.php:215}` | Each uses `in_array($_SESSION['user_role'], [hardcoded list])` instead of `requirePermission()`/`canX()`. A custom role granted equivalent rights through the Roles & Permissions UI is silently denied at these 13 specific checks regardless of what the matrix says. Absorbs and expands the scope of previously-reported finding M-02 (`timesheets/corrections.php`), now documented as one system-wide pattern rather than a single-file instance. | **FIXED — Phase 1, 10 of 13 converted; 3 kept as deliberate business rules.** Converted: `timesheets/corrections.php` → `canApprove('timesheets.approve')` (this ALSO fixed a real bug — `supervisor` holds `can_approve=1` for this slug but was missing from the old hardcoded list, so supervisors couldn't see the approve UI they were entitled to); `employees/view.php` → `canShare('employees.update_links')`; `assets/index.php` → `canCreate('assets.manage')`; `documents/index.php` → `canApprove('documents.verify')`; `leave/{view,index}.php` → `canApprove('leave.approve')` (same supervisor-visibility bug fixed here too); `archive/monthly.php` → `canEdit('archive.generate')`; `temp_employees/{view,add,edit}.php` → `canEdit('temp_employees.edit')`. **Kept as-is (justified):** `leave/apply.php`'s "HR can apply for anyone, others only for themselves" check is a deliberate business rule, not a missing permission (now documented inline); `activity_log/{index,download}.php`'s `$adminRoles` arrays are content filters for which users' activity counts as "admin activity" in a report, not access-control gates. | Phase 1 | **VERIFIED live** for the access-control conversions (smoke-tested, no fatal errors); the two supervisor-visibility bugs fixed as a byproduct are code-verified via the permission-matrix seed data (no supervisor test account exists in this environment to click-test directly). See Regression Test Report and Permission Consistency Report. |
| KOM-041 | Phase 0 — new | No self-service password-reset flow exists on any of the 4 authentication surfaces | Authentication (all surfaces) | `auth/login.php`, `auth/forgot_password.php`, `auth/reset_password.php`, `auth/session.php`, `employee-portal/login.php`, `consultant-portal/login.php` | The only recovery path was an already-authenticated admin manually resetting another admin-surface user's password; employee/consultant portals display "contact HR" text with no link or mechanism at all. | **FIXED — Phase 5, Stage 5.5 (Admin surface only, by user decision).** Built a full forgot-password → emailed one-time token (sha256-hashed at rest, 1-hour expiry, single-use) → reset flow for the Admin surface. Enumeration-resistant (identical generic response for existing/non-existing accounts and for every token-failure mode), rate-limited (5 requests/15 min per IP). Password changes (self-service or reset) now set `users.password_changed_at`, compared against each session's own `login_time` in `auth/session.php` to force re-login on every other active session — the practical substitute for "invalidate other sessions" in a codebase with no central session registry. Employee/Consultant/Temp Portal deliberately kept admin-assisted-only per the Stage 5.5 decision matrix — not addressed this phase. | Phase 5 | **VERIFIED live end-to-end**, including a full separate-session invalidation test: an already-authenticated session (200 OK on a protected page) was confirmed forced to 302-redirect to `login.php?reason=password_changed` immediately after a *second*, independently-triggered reset completed for the same account. See `docs/remediation/Phase5/06-password-recovery-report.md`. |
| KOM-042 | Phase 0 — new | Employee/Consultant/Self-Service portal session cookies never set the Secure flag under any condition | Employee Portal / Consultant Portal / Self-Service | `employee-portal/{login,_session}.php`, `consultant-portal/{login,_session}.php`, `self-service/update.php` | Only the admin surface (`auth/session.php`) conditionally sets `session.cookie_secure` based on HTTPS/`APP_ENV`; the other three surfaces set cookie params inline and never consider HTTPS at all. | **FIXED — Phase 2.** The new shared `bootstrapSession()` (`auth/session_common.php`) applies the identical HTTPS/`APP_ENV`-aware Secure-flag logic to Admin, Employee Portal, Consultant Portal, and Temp Portal alike — one implementation, not four. Self-service's standalone cookie setup (a token flow, not a login session, so deliberately not folded into the shared framework) was given the same conditional Secure flag directly. | Phase 2 | Code-reviewed; the underlying condition is identical to the one already live-verified working on the admin surface prior to this phase. |
| KOM-043 | Phase 0 — new | Consultant portal logout does not destroy the PHP session | Consultant Portal | `consultant-portal/logout.php` | Neither `session_unset()` nor `session_destroy()` is called — only 7 named `cp_*` session keys are individually unset; the underlying session ID/cookie remains valid on the client, with any key outside that specific list surviving "logout." | **FIXED — Phase 2.** Now calls the shared `destroySessionCompletely()` — full `$_SESSION` clear, `session_unset()`, `session_destroy()`, and explicit client-side cookie expiry. | Phase 2 | **VERIFIED live**: logout response now includes `Set-Cookie: PHPSESSID=deleted`, and the old session is confirmed rejected (302) on a subsequent request to a protected page. |
| KOM-044 | Phase 0 — new | Executive Analytics payroll-exposure finding needs re-verification | Reports | `modules/reports/executive.php` | Carried forward from KOM-011 (H-05) as an explicit action item — Audit II's regression check did not re-read this file; status should not be assumed unchanged without a fresh look. | **RESOLVED — superseded by KOM-011's Phase 1 fix.** The re-verification this row asked for happened as part of fixing KOM-011 directly; both rows now describe the same closed gap. | Phase 1 | See KOM-011. |
| KOM-045 | Phase 0 — new | 26 seeded permission slugs are never referenced anywhere in application code (count corrected from 24) | Permissions | `database/{phase1_permissions,phase5_branding_theme}.sql` | Re-verified fresh against the current (post-Phase-4) codebase rather than trusting the Phase 0 list, since several Phase 1–3 fixes incidentally wired up slugs that were originally unused (`leave.apply`, `branding.signatures`, `branding.stamps`, `branding.watermarks` — all now confirmed genuinely in use, removed from this list) while the live schema also grew a few new never-wired slugs in the meantime. The corrected, current list of 26 falls into three groups: **(1) 8 redundant export slugs** (`reports.export`, `employees.export`, `attendance.export`, `leave.export`, `recruitment.export`, `timesheets.export`, `training.export`, `payroll.export`) — every affected module's real export button is already gated by its `.view` permission instead, matching the established convention (see `canExport()` usage across the codebase). **(2) 5 portal.\* slugs** (`portal.access`, `portal.leave`, `portal.attendance`, `portal.payslips`, `portal.requests`) — look intended to let admins toggle which employee-portal features are visible per role, but the portal currently checks only login (`epRequireLogin()`), no permission at all. **(3) 13 miscellaneous slugs** (`archive.lock`, `assets.assign`, `attendance.approve`, `attendance.edit`, `audit.export`, `dashboard.view`, `dashboard.analytics`, `documents.delete`, `email.logs`, `email.send`, `hub.manage`, `users.view`) each tied to a feature that either doesn't exist yet or is already gated by a different, coarser permission. None of this is a live bug — the application functions correctly without any of them. | **REVIEWED AND ACCEPTED, no changes — user decision, 2026-07-13.** Left as a documented, reviewed set of reserved-but-unwired permission slugs; no code or database changes made, consistent with the treatment given to every other genuine feature-gap finding this phase (KOM-088, KOM-090, KOM-097). | Phase 4 (closed, accepted) | Decision recorded; no further action required unless a future phase wants to wire up or remove specific slugs. |
| KOM-070 | Phase 3 — new | Activity Log CSV export references a table that never existed anywhere, live or tracked — every export threw an uncaught exception | Activity Log | `modules/activity_log/download.php` | `SELECT value FROM settings WHERE \`key\`='company_name'` — no `settings` table exists in the live database or in any tracked `.sql` file (this was the 12th of the 12 "undefined tables" under KOM-004; the other 11 turned out to be real, undocumented live tables, but this one was a genuine phantom). Every CSV export from this page threw an uncaught `PDOException` in production. | **FIXED — Phase 3.** Query corrected to `SELECT company_name FROM company_settings WHERE id=1 LIMIT 1`, the table that actually holds this value elsewhere in the codebase. | Phase 3 | **VERIFIED**: query executes without error against the live database; no `settings` table is created or referenced anywhere in the canonical schema. |
| KOM-073 | Phase 4 — new | Unrestricted any-to-any `employees.status` transitions | Employees | `modules/employees/status.php` | Any of the 8 status values could be set directly to any other, with no legality check — only a free-text reason box. Live-verified before the fix: `active → archived → active` both succeeded with zero server-side objection. Reachable: `deceased → active`, `archived → probation`, `terminated → on_leave`, etc. | **FIXED — Phase 4, per explicit user direction.** A transition matrix now governs legal `(old→new)` status pairs for every role except `super_admin`, which may override it (an escape hatch for genuine data-entry corrections, so the matrix can never permanently trap a real record). The status dropdown itself now only lists legal next-statuses for the current role. See `Workflows/01-employee-management-workflow-report.md` §4 for the full matrix. | Phase 4 | **VERIFIED live**: `terminated→suspended` correctly rejected for `hrmanager` (not in the matrix); `terminated→archived` correctly succeeded (in the matrix); `archived→suspended` correctly succeeded for `superadmin` only, via the override path. |
| KOM-076 | Phase 4 — new | No duplicate-person detection for rehires | Employees | `modules/employees/add.php`, `modules/employees/edit.php` | Only `email` was checked for duplicates; `national_id` had no uniqueness check anywhere. A former (resigned/terminated) employee could be re-added as a brand-new, disconnected record — new ID, new employee_number, fresh leave balances, zero link to prior employment history. | **FIXED — Phase 4.** `add.php` now blocks a duplicate `national_id`, identifying the existing record/status and suggesting reactivation instead; `edit.php` got the same check (excluding self). | Phase 4 | **VERIFIED live**: adding a second employee with an in-use `national_id` is correctly rejected, referencing the existing employee number. |
| KOM-083 | Phase 4 — new | Leave approval is single-stage (HR only) in practice, despite being modeled as two-stage | Leave / Approvals | `modules/leave/approve.php`, `config/ApprovalEngine.php` | `ApprovalEngine::workflowConfig()['leave']` defined two stages — Supervisor Review, then HR Approval — but `approve.php` never checked or enforced either; a single `leave.approve` holder resolved the entire request, supervisor step included. | **LOCKED IN as single-stage HR-only — Phase 5, Stage 5.1, user decision 2026-07-13.** `ApprovalEngine::workflowConfig()['leave']` reduced to its actual single `HR Approval` stage; the never-enforced `Supervisor Review` stage removed from the config entirely (the `supervisor` role itself is untouched — it holds an extensive, legitimate permission set independent of leave). No supervisor-facing approvals view was built. See `Phase5/02-leave-approval-completion-report.md`. | Phase 5 | **VERIFIED live**: a new leave application now creates exactly 1 approval stage (`HR Approval`, `hr_manager`); HR approval flips the application, workflow, and its single stage to `approved` together in one action. |
| KOM-084 | Phase 4 — new | Overtime approval had no duplicate-action guard | Timesheets | `modules/timesheets/overtime.php` | Unlike its sibling page `corrections.php` (which already checks `status==='pending'` before acting), `overtime.php` had no equivalent check — an already-approved or already-rejected overtime record could be re-approved or flipped after the fact with no protection. | **FIXED — Phase 4.** Added the same pending-status guard `corrections.php` already uses, plus a clear error message when a record has already been decided. | Phase 4 | **VERIFIED live**: approved a test overtime record; a second action (attempting to reject the same record) was correctly rejected with the status unchanged. |
| KOM-086 | Phase 4 — new | 32 orphaned `payroll_deductions` rows (nearly half the table) reference nonexistent employees | Payroll | `payroll_deductions` | Has an `ON DELETE CASCADE` FK to `employees`, yet 32 of 67 rows reference `employee_id` values that don't exist in the current 13-row `employees` table. `employee_savings`/`payslips` (same FK pattern) have zero orphans. All 32 share one identical timestamp and `amount=NULL`, indicating a bulk demo-data insert whose referenced employees were later removed without the cascade firing (most likely via a cleanup that had `FOREIGN_KEY_CHECKS` disabled, or the FK was added after these rows already existed). | **FIXED — Phase 4, per explicit user direction.** All 32 rows backed up to `database/backups/orphaned_payroll_deductions_20260712.tsv` before deletion, then removed. | Phase 4 | **VERIFIED**: 35 legitimate rows remain (67 − 32), zero orphaned rows confirmed by the same query that found them. |
| KOM-089 | Phase 4 — new | Consultant deletion had no safety confirmation, unlike the established employee-delete pattern | Consultants | `modules/consultants/delete.php` | A single-click, JS `confirm()`-only instant hard delete — no server-side impact preview, no type-to-confirm safeguard — despite cascading to a consultant's entire `consultant_attendance`/`consultant_scopes` history. Inconsistent with `modules/employees/delete.php`'s already-established pattern for the exact same class of action. | **FIXED — Phase 4.** Rewrote `delete.php` to match the employees pattern: a GET confirmation page with an impact-count summary, requiring the exact `consultant_number` typed to confirm before the delete proceeds. | Phase 4 | **VERIFIED live**: confirmation page loads with correct impact summary; wrong confirmation text correctly rejected (record unchanged); correct consultant number correctly deletes. Phase 2 regression's consultant-delete test updated to match (not a product regression — the old test exercised the old, now-intentionally-changed instant-delete behavior); re-verified 29/29. |
| KOM-090 | Phase 4 — new | Attendance-method UI claims temp employees can clock in via kiosk or timesheet, but neither path actually captures any data | Temp Employees | `modules/temp_employees/{add,edit,view,index,timesheet,attendance_entry}.php` | The "Kiosk Only" attendance method was described as "Clock in/out via the kiosk tablet," but `modules/attendance/kiosk.php` looks employees up only in the `employees` table (`getEmployeeByNumber()`) — a `KOM-TMP-####` number is always rejected as "not found." The "Timesheet Only" option downloads a blank, printable Excel/PDF form with no digital re-entry point anywhere. No `temp_attendance` (or equivalent) table exists in the schema. | **FIXED — Phase 5, Stage 5.8 (user decision: supervisor/HR-entered digital capture, not a kiosk path).** New `temp_attendance` table (one row per employee per day). New `modules/temp_employees/attendance_entry.php` reuses `timesheet.php`'s project/site/week selection and renders an editable weekly grid, pre-filled from any existing entries, saved via a single POST (`temp_employees.edit` gated). `index.php`/`timesheet.php` link to it; `view.php` shows a 14-day recent-attendance summary. Kiosk remains correctly unsupported for temp employees (no identity-verification path built — out of this phase's authorized scope); UI copy updated again to point at Attendance Entry instead of describing a dead end. Also resolves KOM-058. | Phase 5 | **VERIFIED live**: entered attendance for 2 days for a real active employee, confirmed correct DB rows and `entered_by`; reload correctly pre-fills saved values; `view.php`'s summary card shows the correct total; audit log entry confirmed. Test data removed after verification. See `Phase5/09-temp-attendance-capture-report.md`. |
| KOM-091 | Phase 4 — new | Temp employee deletion had no safety confirmation, unlike the established employee/consultant-delete pattern | Temp Employees | `modules/temp_employees/delete.php` | Same class of gap as KOM-089: a single-click, JS `confirm()`-only instant hard delete with no server-side confirmation safeguard — a single accidental click permanently erased the record (contract dates, rate, notes, portal access) with no way to recover it. | **FIXED — Phase 4.** Rewrote `delete.php` to match the employees/consultants pattern: a GET confirmation page requiring the exact `employee_number` typed to confirm before the delete proceeds. | Phase 4 | **VERIFIED live**: confirmation page loads; wrong confirmation text correctly rejected (record unchanged); correct employee number correctly deletes; re-verified against Phase 1 (20/20) and Phase 2 (29/29) regression. |
| KOM-094 | Phase 4 — new | Employee Hub request notifications stored with an invalid `notifications.type` — same bug class as KOM-007's original defect | Notifications / Employee Portal | `employee-portal/hub.php` | `notifyRole('hr_manager', 'hub_request', ...)` / `notifyRole('super_admin', 'hub_request', ...)` — but `notifications.type` is a 4-value ENUM (`info`/`success`/`warning`/`danger`) with no `'hub_request'` member. With `STRICT_TRANS_TABLES` not set on this database, MySQL silently coerces the invalid value to an empty string rather than erroring. Live-verified against real (pre-existing, not test) data: every "New Hub Request" notification ever created had `type=''`. No current UI reads `type` for icon/color coding, so this had zero visible symptom — but it's not what was intended, matching the exact bug class self-caught and corrected in Workflow Group 1's own new code. | **FIXED — Phase 4.** Both calls changed to `'warning'`, matching the "requires HR attention" convention already used by every other `notifyRole()` call in the codebase (`employees/edit.php`, `employees/status.php`, `leave/apply.php`). | Phase 4 | **VERIFIED live**: submitted a real hub request post-fix — resulting `notifications` row has `type='warning'`. |

---

## LOW (27)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase |
|---|---|---|---|---|---|---|---|
| KOM-046 | L-01 | Pagination values interpolated directly into SQL | Employees, Attendance, Timesheets, Recruitment, Onboarding, Training | 6 `index.php` files | Values are int-cast before use (not currently exploitable) but deviate from the prepared-statement standard used elsewhere. | Open | Phase 4 |
| KOM-047 | L-02 | Approval-cancellation reason silently corrupted | Approvals | `config/ApprovalEngine.php` (`cancel()`) | Uses `+` instead of string concatenation (MySQL numeric coercion); currently dead/unused code. | **FIXED — Phase 5, Stage 5.2.** Corrected to a proper multi-argument `CONCAT()`. `cancel()` remains dead code (no call site), kept rather than removed since it's a generically useful method for the 3 real workflow types (promotion/transfer/termination) independent of this stage's dormant-type cleanup (4 never-instantiated workflow types — `overtime`/`correction`/`payroll_run`/`document` — removed from `ApprovalEngine::workflowConfig()` per user decision). See `Phase5/03-approvalengine-workflow-resolution-report.md`. | Phase 5 |
| KOM-048 | L-03 | Missing-documents report runs one query per employee (N+1) | Documents | `modules/documents/missing.php` | Loops per-employee instead of a single joined query; scales linearly with headcount. | Open | Phase 4 |
| KOM-050 | L-05 | No CSRF token on consultant portal login form | Consultant Portal | `consultant-portal/login.php` | Login-CSRF risk (forces victim to authenticate as attacker-controlled account); lower severity, doesn't compromise the victim's own account. | **FIXED — Phase 2.** Login form now carries and verifies a CSRF token, matching the admin login's existing pattern. | Phase 2 |
| KOM-051 | L-06 | Archive "Lock" control silently no-ops | Archive | `modules/archive/monthly.php` | Posts a `lock_id` field the handler never reads; visibility gated by hardcoded role list (see also KOM-040). | Open | Phase 4 |
| KOM-052 | L-07 | No brute-force lockout on employee-portal login | Employee Portal | `employee-portal/login.php` | No failed-attempt tracking, unlike the admin login's 5-attempt/15-min lockout. | **FIXED — Phase 2.** New `portalLoginBlocked()`/`recordPortalLoginFailure()` (`config/functions.php`) reuse the existing `audit_logs` table (no schema change) to rate-limit by IP: 5 failures in 15 minutes blocks further attempts. Applied to both employee-portal and consultant-portal logins. | Phase 2 |
| KOM-053 | L-08 | Error display defaults to on | Config | `config/config.php` | `APP_ENV` defaults to `'development'` unless explicitly set otherwise. | Open | Phase 4 |
| KOM-054 | L-09 | Standing deployment hardening items unaddressed | Environment | `config/config.php`, `.htaccess` | Blank root DB password, default admin password, no HTTPS redirect, no CSP header. | Open | Phase 4 |
| KOM-055 | NL-01 | Replaced branding assets never deleted from disk | Settings / Branding | `modules/settings/branding.php` | No `unlink()` on update/delete; unbounded storage growth. | Open | Phase 4 |
| KOM-056 | NL-02 | Server-generated date interpolated directly into SQL | Dashboard | `dashboard.php` | Not exploitable (server-generated value), but deviates from prepared-statement standard. | Open | Phase 4 |
| KOM-057 | NL-03 | Dead query wastes a DB round-trip on every temp-employee list page load | Temp Employees | `modules/temp_employees/index.php` | Throwaway COUNT statement's `PDOStatement` object assigned then immediately overwritten. | Open | Phase 4 |
| KOM-058 | NL-04 | Temp-employee timesheet feature is a blank template, not a data-capture tool | Temp Employees | `modules/temp_employees/timesheet.php` | Renders a printable grid only; no persistence, hours calculation, or payroll linkage — not a bug, a completeness gap. | **FIXED — Phase 5, Stage 5.8.** Resolved together with KOM-090 — see that row for full detail. The printable grid in `timesheet.php` is retained for manual/signature record-keeping, but a real persistence mechanism (`temp_attendance` table + `attendance_entry.php`) now exists alongside it. Payroll linkage remains out of scope (a separate decision, not requested this stage). | Phase 5 | See KOM-090. |
| KOM-059 | NL-05 | Temp employee position modeled as free text, not FK | Temp Employees | `database/phase8_temp_employees.sql` | Inconsistent with `modules/employees`'s normalized `positions` table; complicates future cross-module reporting. | Open | Phase 4 |
| KOM-060 | NL-06 | Letterhead header/footer HTML fields saved but never used | Settings / Branding / Documents | `modules/settings/branding.php`, `config/DocumentEngine.php` | `company_letterheads.header_html`/`footer_html` captured on save, never read by the rendering engine. | Open | Phase 4 |
| KOM-061 | NL-07 | Core document tables have no traceable schema origin | Database | `doc_templates`, `doc_categories`, `generated_documents`, `doc_template_versions` | Compounds KOM-004 — these tables' true structure isn't reproducible from the repository at all. | **FIXED — Phase 3 (closed with KOM-004).** All four tables' exact structure is now in `database/schema.sql`, extracted verbatim from the live database; `doc_categories`' seed data (10 rows, previously undocumented anywhere) is now in `database/seeds/002_doc_categories.sql`. | Phase 3 (paired with KOM-004) |
| KOM-062 | Phase 0 — new | Self-service per-link CSRF token compared with non-constant-time `!==` | Self-Service | `self-service/update.php` | Uses plain string comparison instead of `hash_equals()`, unlike the shared `verifyCsrfToken()` helper used elsewhere. Low practical risk given the token is single-use and per-link. | **FIXED — Phase 2.** Comparison now uses `hash_equals()`, matching the standard pattern used everywhere else. | Phase 2 |
| KOM-063 | Phase 0 — new | Password minimum length inconsistent across admin-reset paths | Users / Auth | `modules/users/index.php`, `auth/change_password.php` | Admin-initiated reset modal allows a 6-character minimum; self-service change requires 8. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-064 | Phase 0 — new | `requireRole()` authorization helper defined with zero call sites | Config | `config/functions.php` | Dead code — a second, parallel hardcoded-list authorization primitive that nothing in the codebase actually calls. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-065 | Phase 0 — new | `employee_skills` table defined but never used anywhere in code | Database | `database/schema.sql` | Has a `CREATE TABLE`, is never seeded or queried; `employee_qualifications`/`employee_work_history` appear to be its functional successors. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-066 | Phase 0 — new | Double session-ID regeneration on first post-login admin page load | Auth | `auth/login.php`, `auth/session.php` | `login.php` regenerates once at successful auth but never stamps `last_regenerated`; the very next page load through `session.php` regenerates a second time. Harmless, but undocumented and worth a single stamp-on-login fix for clarity. | **FIXED — Phase 2.** The new shared `regenerateSessionOnLogin()` both regenerates AND stamps the rotation timestamp in one call, used by `auth/login.php` — the next `bootstrapSession()` call sees an already-fresh timestamp and does not redundantly rotate again. | Phase 2 |
| KOM-067 | Phase 0 — new | Employee-portal logout omits `session_unset()` before `session_destroy()` | Employee Portal | `employee-portal/logout.php` | Functionally still clears the session store, but skips the explicit in-memory `$_SESSION` unset step that `auth/logout.php` performs — a mechanical inconsistency across the 3 logout implementations (see also KOM-043). | **FIXED — Phase 2.** All logout endpoints (admin, employee, consultant, temp) now call the identical shared `destroySessionCompletely()` — no more per-surface variation in teardown steps. | Phase 2 |
| KOM-075 | Phase 4 — new | Exit statuses didn't require an exit date | Employees | `modules/employees/status.php` | `exit_date` was optional server-side for every status, including `resigned`/`terminated`/`deceased` — the UI only conditionally displayed the field via JS, nothing enforced it being filled in. | **FIXED — Phase 4.** Server-side validation now requires `exit_date` for `resigned`/`terminated`/`deceased`, matching the UI's own existing definition of "exit" statuses. | Phase 4 |
| KOM-077 | Phase 4 — new | Employee-edit audit log didn't capture actual changed fields | Employees | `modules/employees/edit.php` | `auditLog()`'s `new_value` only ever contained `first_name`/`last_name` — a transfer (department/supervisor) or promotion (position/salary) left no independently reconstructable record of the new state in the field meant to show it. | **FIXED — Phase 4.** `new_value` now includes department, position, supervisor, employment type, salary, and date fields. | Phase 4 |
| KOM-080 | Phase 4 — new | Department/position disable had no deletion-protection visibility | Departments / Positions | `modules/settings/index.php` | Disabling a department or position had no check for how many employees/positions currently reference it — not destructive (existing assignments keep functioning; `getDepartments()`/`getPositions()` already filter `is_active=1`), but silent and uninformative, unlike Employee Delete's full cascade-impact preview. | **FIXED — Phase 4.** Disable actions now count active employees/positions still referencing the record and include the count in the confirmation message. | Phase 4 |
| KOM-096 | Phase 4 — new | "Documents Expiring Soon" banner never showed a separator between entries | Documents | `modules/documents/index.php` | `if (!end($expiringSoon) === $d) echo '; ';` is broken by operator precedence — `!` binds to `end($expiringSoon)` (an array) before the comparison, so the condition is `false === $d` (a strict-type mismatch against an array), which is never true. Every entry in the banner ran together with no separator at all (e.g. "...Doc A (18 Jul 2026)Jane Smith — Doc B..."). Live-verified with 2 disposable test documents: no separator appeared before the fix. | **FIXED — Phase 4.** Corrected to `if ($d !== end($expiringSoon)) echo '; ';`, comparing the current item directly to the array's actual last element. **VERIFIED live**: the same 2 test documents now render with a `; ` separator between them and none after the last; test data removed after verification. | Phase 4 |
| KOM-097 | Phase 4 — new | QR code on generated documents links to a verification page that doesn't exist | Documents | `config/DocumentEngine.php`, `modules/documents/templates.php` | A template's "Show QR Code" option encodes a URL to `/verify-doc.php?ref=...` — no such file exists anywhere in the repository (confirmed: nothing at the site root; the only similarly-named file, `modules/documents/verify.php`, is a completely unrelated authenticated internal action for marking an *uploaded* document as verified, unrelated to the public QR-scan flow for *generated* documents). Anyone scanning the QR code would reach a 404. Confirmed dormant, not actively broken today: 0 of the 47 live templates currently have `show_qr_code` enabled. | **Left documented, not built — user decision, 2026-07-13.** Building a real public, unauthenticated verification page is a genuine feature requiring a decision on what to expose to an anonymous third party and whether to rate-limit it — out of scope for this pass. No code change made. | Phase 4 (deferred) |
| KOM-099 | Phase 4 — new | Dashboard's "Pending Approvals" header total disagreed with its own visible rows | Dashboard | `dashboard.php` | `$totalPending` summed only 4 of the 5 categories the card actually displays (Leave, Timesheet Corrections, Profile Updates, Overtime) — `$pendingRecruitment` was fetched, shown as the card's 5th row ("New Applications"), and never added to the header badge. Live-verified: header showed "4 pending" while the 5 visible rows summed to 5 (3 leave + 1 correction + 1 recruitment). | **FIXED — Phase 4.** `$pendingRecruitment` added to the sum. | Phase 4 | **VERIFIED live**: header badge now correctly shows "5 pending," matching the sum of all 5 visible rows. |

---

## Register Totals

| Severity | Count |
|---|---|
| Critical | 13 |
| High | 28 |
| Medium | 31 |
| Low | 27 |
| **Total** | **99** |

**By status (end of Phase 1):** 12 Fixed (KOM-001, KOM-010, KOM-011, KOM-014, KOM-015, KOM-018, KOM-019, KOM-021, KOM-023, KOM-032, KOM-036, KOM-040), 1 Resolved as duplicate (KOM-044, superseded by KOM-011), 1 still partially-changed (KOM-031, unrelated to Phase 1's authorization scope), 53 Open.

**By status (end of Phase 2):** +14 more Fixed (KOM-002, KOM-012, KOM-013, KOM-017, KOM-026, KOM-027, KOM-029, KOM-042, KOM-043, KOM-050, KOM-052, KOM-062, KOM-066, KOM-067) — **26 Fixed total**, 1 Resolved as duplicate, 1 partially-changed (KOM-031, still open, not an authentication/session finding), **39 Open**.

**By status (end of Phase 3):** +7 more Fixed (KOM-004, KOM-005, KOM-024, KOM-061, and 3 newly-discovered-and-fixed-same-phase findings KOM-068, KOM-069, KOM-070) — **33 Fixed total** out of 70 (register grew from 67 to 70 this phase). KOM-023 also received a source-level fix this phase (its seed files, not just the live database) but was already counted as Fixed at Phase 1 close, so it is not double-counted here. 1 finding retargeted from Phase 3 to Phase 4 (KOM-045 — a slug-by-slug product decision, not a schema-integrity defect, out of this phase's charter scope). Totals: **33 Fixed**, 1 Resolved as duplicate, 1 partially-changed (KOM-031), **35 Open**.

**Phase 4, in progress — after Workflow Group 1 (Employee Management):** +7 Fixed (KOM-071, KOM-074, KOM-075, KOM-076, KOM-077 discovered-and-fixed same round; KOM-072 and KOM-073 initially flagged as business-policy decisions, then fixed per explicit user direction to proceed with both) — **40 Fixed total** out of 77 (register grew from 70 to 77).

**Phase 4, in progress — after Workflow Group 2 (Department & Position Management):** +3 more Fixed (KOM-078, KOM-079, KOM-080 — all discovered and fixed this same round) — **43 Fixed total** out of 80 (register grew from 77 to 80).

**Phase 4, in progress — after Workflow Group 3 (Leave Management):** +3 more Fixed (KOM-081, KOM-082 discovered-and-fixed this round; KOM-007 — pre-existing since Phase 0, correctly out of scope for Phases 1–3, closed here) — **46 Fixed total** out of 83 (register grew from 80 to 83, +3 new findings: KOM-081, KOM-082, KOM-083). 1 new finding deliberately left Open, flagged for a product/business-policy decision (KOM-083 — leave approval is single-stage HR-only, not two-stage as modeled).

**Phase 4, in progress — after Workflow Group 4 (Attendance & Timesheets):** +2 more Fixed (KOM-003 — pre-existing since Phase 0, kiosk remote clock-in impersonation, correctly out of scope for Phase 1, closed here; KOM-084 — discovered-and-fixed this round: overtime approval had no duplicate-action guard) — **48 Fixed total** out of 84 (register grew from 83 to 84, +1 new finding: KOM-084).

**Phase 4, in progress — after Workflow Group 5 (Payroll):** +1 more Fixed (KOM-030 — pre-existing since Phase 0, payroll run create/finalize/publish race condition, correctly out of scope for Phase 3, closed here with a genuine concurrency test) — **49 Fixed total** out of 86 (register grew from 84 to 86, +2 new findings: KOM-085, KOM-086).

**Phase 4, KOM-085/KOM-086 decisions (user-confirmed, 2026-07-12):** KOM-085 accepted as designed — deductions/savings remain record-keeping only, no code change. KOM-086 fixed per explicit user direction — the 32 orphaned rows were backed up (`database/backups/orphaned_payroll_deductions_20260712.tsv`) then deleted; 0 orphans remain. **50 Fixed total** out of 86.

**Phase 4, in progress — after Workflow Group 6 (Performance Management):** +1 more Fixed (KOM-049 — pre-existing since Phase 0, originally reported Low-severity, investigation revealed the real root cause was far more severe: 3 nonexistent columns causing a 100%-failure fatal crash, severity corrected to Critical, closed here) — **51 Fixed total** out of 86 (no change in register total — this was a severity correction of an existing finding, not a new one).

**Phase 4, in progress — after Workflow Group 7 (Recruitment):** +1 more Fixed (KOM-087 — discovered and fixed this round: no way to create a recruitment application existed anywhere) — **52 Fixed total** out of 88 (register grew from 86 to 88, +2 new findings: KOM-087, KOM-088). 1 new finding deliberately left Open (KOM-088 — no employee-conversion step despite the schema being built for it, a feature build flagged for decision).

**KOM-088 decision (user-confirmed, 2026-07-12):** left as a manual, disconnected step — no code change, deferred rather than built.

**Phase 4, in progress — after Workflow Group 8 (Training):** +1 more Fixed (KOM-008 — pre-existing since Phase 0, originally reported as a single High-severity column mismatch, investigation revealed the entire Training module was fatally broken in 3 separate places plus 2 silent display bugs, severity corrected to Critical, closed here) — **53 Fixed total** out of 88 (no change in register total — this was a severity correction of an existing finding, not a new one).

**Phase 4, in progress — after Workflow Group 9 (Consultant Module):** +1 more Fixed (KOM-089 — discovered and fixed this round: consultant deletion had no server-side impact preview or type-to-confirm safeguard, unlike the established employees-module pattern, despite cascading to all attendance/scope history) — **54 Fixed total** out of 89 (register grew from 88 to 89, +1 new finding: KOM-089).

**Phase 4, in progress — after Workflow Group 10 (Temporary Employee Module):** +4 more Fixed (KOM-020 — pre-existing since Phase 0/Audit II, reflected XSS in the export status banner, correctly out of scope for Phase 2, closed here; KOM-091 — discovered and fixed this round: temp employee deletion had no safety confirmation, same class as KOM-089; KOM-092 — discovered and fixed this round: `auditLog()` called with the wrong argument order across the whole module, corrupting `record_id`/`old_value` for every create/edit/delete; KOM-090 — discovered this round, partially fixed per user decision: misleading Kiosk/Timesheet UI copy corrected, but the underlying attendance-capture feature itself deferred to a future phase) — **58 Fixed total** out of 92 (register grew from 89 to 92, +3 new findings: KOM-090, KOM-091, KOM-092; KOM-020 was pre-existing and not counted twice).

**Phase 4, in progress — after Workflow Group 11 (Notifications):** +3 more Fixed (KOM-093 — discovered and fixed this round: the notification bell rendered `title`/`message` via unescaped `innerHTML`, and the employee-portal Hub's self-service request form fed a low-privilege employee's raw free text straight into a notification sent to every `hr_manager`/`super_admin` — a genuine stored-XSS privilege-escalation chain, severity Critical; KOM-095 — discovered and fixed this round: `ApprovalEngine::act()` never notified the requester of an approve/reject decision for any workflow type, a systemic completeness gap affecting every termination/transfer/promotion request; KOM-094 — discovered and fixed this round: the same invalid-ENUM `notifications.type` bug class self-corrected in Workflow Group 1, found still present in `employee-portal/hub.php`) — **61 Fixed total** out of 95 (register grew from 92 to 95, +3 new findings: KOM-093, KOM-094, KOM-095). Training, Recruitment, and admin-initiated password-reset notifications, plus any scheduled/"reminder" notification mechanism, were confirmed to not exist anywhere in the codebase (no cron/scheduler exists at all) — documented as completeness gaps, not built, since introducing them is new feature scope rather than a fix to existing broken behavior.

**Phase 4, in progress — after Workflow Group 12 (Documents Generation Lifecycle):** +3 more Fixed (KOM-006 — pre-existing since the baseline audit, Critical, "not yet live-verified" — confirmed live and closed here: all 4 branding-asset `.htaccess` files were blocking every letterhead/signature/stamp/watermark image with an unconditional `Deny from all`, making the entire document-branding feature non-functional; KOM-022 — pre-existing since the baseline audit, High, correctly out of Phase 1 scope — closed here: template bodies are now sanitized at save time via a new DOMDocument-based sanitizer, closing the stored-XSS-via-template-authoring gap; KOM-096 — discovered and fixed this round: a broken operator-precedence check meant the "Documents Expiring Soon" banner never showed a separator between entries) — **64 Fixed total** out of 97 (register grew from 95 to 97, +2 new findings: KOM-096, KOM-097). KOM-097 (QR code on generated documents links to a verification page, `/verify-doc.php`, that doesn't exist anywhere) left deferred per user decision — documented only, since 0 of the 47 live templates currently enable QR codes (dormant, not actively broken today) and building a real public verification page is a genuine feature requiring its own design decisions. Totals so far: **64 Fixed** (1 of which — KOM-090 — remains partial), 1 Resolved as duplicate, 1 partially-changed (KOM-031), 1 Accepted as designed (KOM-085), 2 Deferred (KOM-088, KOM-097), **31 Open**.

**Phase 4, in progress — after Workflow Group 13 (Reports & Dashboards Consistency):** +2 more Fixed (KOM-099 — discovered and fixed this round: the Dashboard's "Pending Approvals" header total silently disagreed with its own visible rows, omitting recruitment applications from the sum; KOM-098 — discovered and partially fixed this round: `attendance.is_absent` is dead code everywhere it's read — never written by any code path — so every "Absent" figure across the Dashboard and Reports has been structurally guaranteed to show 0/silently wrong since the column was introduced. The two *daily* instances (Dashboard's "Absent Today"/7-day trend, the Attendance module's day summary) were fixed with an honest active-employees-minus-clocked-in calculation; the *monthly/period* instances (Reports' attendance report, executive KPIs, timesheet report) were left documented, not fixed, since a correct multi-day figure needs a working-day/holiday calendar that doesn't exist anywhere in this codebase) — **66 Fixed total** out of 99 (register grew from 97 to 99, +2 new findings: KOM-098, KOM-099). Totals so far: **66 Fixed** (2 of which — KOM-090, KOM-098 — remain partial), 1 Resolved as duplicate, 1 partially-changed (KOM-031), 1 Accepted as designed (KOM-085), 2 Deferred (KOM-088, KOM-097), **31 Open**.

**Phase 4, close-out — KOM-045 (unused permission slugs) resolved, 2026-07-13:** all 13 workflow groups now complete. KOM-045 (deferred from Phase 3, out of that phase's schema-integrity charter scope) re-verified fresh against the post-Phase-4 codebase, correcting its count from 24 to 26 genuinely-unused slugs, and closed per explicit user decision: reviewed and accepted as a documented, reserved set — no code or database changes made. Totals: **66 Fixed**, 1 Resolved as duplicate, 1 partially-changed (KOM-031), **2 Accepted as designed** (KOM-085, KOM-045), 2 Deferred (KOM-088, KOM-097), **31 Open**. No register-total change (KOM-045 is a status update to an existing finding, not a new one). This is the final status update before the Phase 4 Completion Report.

**Phase 5, in progress — Stage 5.0/open-findings audit, 2026-07-13:** a full line-by-line re-read of all 99 rows, cross-checked against each severity section's own header count, found the "Open" tally above (31) had drifted from the true figure. Correction: **KOM-035** is the identical defect, same files, as **KOM-092** (fixed in Phase 4 Workflow Group 10) but was never cross-referenced and closed — resolved now as superseded by KOM-092, no code change. True open count at Phase 5 start: **28**, not 31. See `Phase5/01-phase5-open-findings-scope.md` for the full corrected inventory and the 9-point decision matrix (all resolved, user sign-off 2026-07-13).

**Phase 5, Stage 5.1 (Leave Approval Model), 2026-07-13:** **KOM-083** resolved — locked in single-stage HR-only leave approval as the permanent design (the never-enforced Supervisor Review stage removed from `ApprovalEngine::workflowConfig()`); live-verified with a real leave application end to end. Combined with KOM-035's closure above: **67 Fixed total**, **2 Resolved as duplicate** (KOM-044, KOM-035), 1 partially-changed (KOM-031), 2 Accepted as designed (KOM-085, KOM-045), 2 Deferred (KOM-088, KOM-097), **26 Open**. No register-total change (both are status updates to existing findings, not new ones).

**Phase 5, Stage 5.2 (ApprovalEngine Dormant Workflow Types), 2026-07-13:** **KOM-047** fixed — `cancel()`'s `+`-vs-concatenation bug corrected; the 4 never-instantiated workflow types (`overtime`, `correction`, `payroll_run`, `document`) removed from `ApprovalEngine::workflowConfig()` per user decision, since each already has its own working, independently-built approval mechanism. Live-verified: Approvals UI type filter now shows exactly the 4 real types; a full termination-workflow round trip (create → approve → employee status change → initiator notification) re-confirmed working correctly after the config restructuring. **68 Fixed total**, 2 Resolved as duplicate, 1 partially-changed (KOM-031), 2 Accepted as designed, 2 Deferred, **25 Open**.

**Phase 5, Stage 5.3 (Working-Day & Holiday Calendar), 2026-07-13:** completes the deferred half of **KOM-098** — built the working-day/holiday calendar infrastructure (`work_calendar_settings`/`work_calendar_holidays` tables, `isWorkingDay()`/`countWorkingDays()`/`getWorkingDaysBetween()`/`getNextWorkingDay()`, new admin UI at `modules/settings/calendar.php`) and used it to correctly compute the monthly/period attendance figures in Reports that Phase 4 had left documented rather than fixed. Also wired the calendar into `leave/apply.php`'s day-count calculation (previously hardcoded Mon–Fri only, no holiday awareness) and improved the daily Absent figures to show 0 on a non-working day rather than the full headcount. 17/17 calendar unit tests pass. KOM-098 no longer carries a "partial" qualifier — **1 partial remaining** (KOM-090, temp employee attendance capture, addressed in Stage 5.8). Fixed total unchanged at **68** (KOM-098 was already counted within Fixed as a partial; this stage completes it rather than adding a new finding). 2 Resolved as duplicate, 1 partially-changed (KOM-031), 2 Accepted as designed, 2 Deferred, **25 Open**.

**Phase 5, Stage 5.4 (Scheduled Task Infrastructure), 2026-07-13:** built a lightweight, shared-hosting-compatible scheduler (`cron/run.php` + `bootstrap.php` + 4 task files + `scheduled_task_locks`/`scheduled_task_runs` tables) — no cron/scheduled-task mechanism existed anywhere in this codebase before now. Live-verified: web access blocked (403, two independent layers); CLI run processes all 4 registered tasks; overlapping-run lock correctly rejects a second concurrent invocation; a stale (>30 min) lock is auto-recovered; a forced failure in one task is correctly isolated, recorded, and does not block the other 3; both functional tasks (`expire_tokens`, `cleanup_safe_temp_files`) correctly process real disposable test data and are idempotent. No specific register finding maps to this stage — new infrastructure for Stage 5.5/5.6 to build on, not a fix to a documented defect. No register-total change. This total will continue to update as each subsequent Phase 5 stage completes.

**Phase 5, Stage 5.5 (Self-Service Password Recovery, Admin Surface Only), 2026-07-13:** **KOM-041** fixed — built a full forgot-password → emailed one-time token → reset flow for the Admin surface (enumeration-resistant, rate-limited, single-use, sha256-hashed-at-rest token), plus a `password_changed_at`-vs-session-`login_time` mechanism in `auth/session.php` that forces re-login on every other active session after a password change or reset. Live-verified end-to-end, including a genuine two-session test: an already-authenticated session was confirmed to receive a forced 302 redirect immediately after a second, independently-triggered reset completed for the same account. Employee/Consultant/Temp Portal deliberately out of scope this phase, per the Stage 5.5 decision matrix. A minor self-introduced `email_logs.type` enum gap (missing a `'password_reset'` value, causing silent coercion to an empty string under non-strict SQL mode) was caught during live testing and fixed in the same stage — see `Phase5/06-password-recovery-report.md` §5 for full disclosure. **69 Fixed total**, 2 Resolved as duplicate, 1 partially-changed (KOM-031), 2 Accepted as designed, 2 Deferred, **24 Open**.

**Phase 5, Stage 5.6 (Deferred Notification Workflows), 2026-07-13:** filled in `cron/tasks/send_reminders.php`'s real logic — 9 reminder categories (employee contract expiry, probation ending, temp employee/consultant contract ending, employee document expiry, training starting soon, recruitment interview tomorrow, leave approval sitting unactioned, payroll run finalized-not-published), addressing the Training/Recruitment/scheduled-reminder completeness gaps documented in Phase 4 Workflow Group 11. The two "sitting unactioned" categories use the Stage 5.3 working-day calendar rather than raw calendar days. A design gap was found and fixed during live testing: the original day-threshold-only design assumed a once-daily cron run, but `cron/README.md` itself recommends 15-30 minute cadence — added `reminder_notifications_log`, a per-day dedup table, so every reminder is correct at any scheduler frequency. Live-verified: a full 9-category test run correctly fired every category exactly once; an immediate second run (fresh CLI process, same day) correctly fired zero duplicates. No specific finding — new infrastructure completing previously-documented gaps, same treatment as Stage 5.4. No register-total change.

**Phase 5, Stage 5.7 (Recruitment-to-Employee Conversion), 2026-07-13:** **KOM-088** fixed — built a guided conversion (per user decision, not a separate parallel system): a "Convert to Employee" action on a `selected` recruitment application pre-fills the existing Add Employee form via `?from_application=<id>` and sets `converted_to_employee_id` on save, re-validated against the same convertible-state condition at POST time to prevent double-conversion. Live-verified end-to-end including the re-conversion-blocked case and confirmation that the baseline (non-conversion) Add Employee flow is unaffected. **70 Fixed total**, 2 Resolved as duplicate, 1 partially-changed (KOM-031), 2 Accepted as designed, **1 Deferred** (KOM-097), **23 Open**.

**Phase 5, Stage 5.8 (Temporary Employee Attendance Capture), 2026-07-13:** **KOM-090** (completing its Phase 4 partial fix) and **KOM-058** both fixed — built supervisor/HR-entered digital attendance capture (per user decision, not a kiosk path): a new `temp_attendance` table and `modules/temp_employees/attendance_entry.php`, reusing the existing project/site/week selector, with pre-filled editable entry and a `view.php` recent-attendance summary. Kiosk remains correctly unsupported for temp employees. Live-verified: real 2-day entry, correct pre-fill on reload, correct summary total, audit trail confirmed. **72 Fixed total**, 2 Resolved as duplicate, 1 partially-changed (KOM-031), 2 Accepted as designed, 1 Deferred (KOM-097), **21 Open**.

**No finding has a Date Closed yet** — "Fixed" here means the code/data change is live and regression-tested against the running application; "Closed" is reserved for after a full acceptance pass, which is a Phase 5/governance step. See the Phase 1, Phase 2, Phase 3 Completion Reports, and (once complete) the Phase 4 Completion Report for the full accounting of what changed and why each fix is marked Fixed rather than Closed.

---

## Change Log for This Document

| Date | Change | Author |
|---|---|---|
| 2026-07-11 | Initial consolidated register compiled from Audit I (34 findings), Audit II (21 findings + 13-item regression check), and Phase 0 baseline inventory (12 newly discovered items) | Remediation Program — Phase 0 |
| 2026-07-11/12 | Phase 1 close: 12 findings marked Fixed with live regression evidence (KOM-001, KOM-010, KOM-011, KOM-014, KOM-015, KOM-018, KOM-019, KOM-021, KOM-023, KOM-032, KOM-036, KOM-040), 1 marked Resolved as duplicate (KOM-044), KOM-002 explicitly reassigned out of Phase 1 scope with reasoning recorded. See Change Control Log (`Regression/change-control-template.md`) for the full file-by-file record. | Remediation Program — Phase 1 |
| 2026-07-11/12 | Phase 2 close: 14 more findings marked Fixed with live regression evidence (KOM-002, KOM-012, KOM-013, KOM-017, KOM-026, KOM-027, KOM-029, KOM-042, KOM-043, KOM-050, KOM-052, KOM-062, KOM-066, KOM-067) — every authentication/session-security finding named in the Phase 2 charter, plus every related item discovered during Phase 0 baseline inventory. See Change Control Log for the full file-by-file record and the Phase 2 Regression Test Report for the 29-case live test suite. | Remediation Program — Phase 2 |
| 2026-07-12 | Phase 3 close: 4 findings marked Fixed (KOM-004, KOM-005, KOM-024, KOM-061), KOM-023 given an additional source-level fix (its Phase 1 Fixed status unchanged), 3 new findings discovered and fixed the same phase (KOM-068, KOM-069, KOM-070), 1 finding retargeted out of Phase 3 scope (KOM-045 → Phase 4). Register total grew from 67 to 70 findings. See Change Control Log for the full file-by-file record and `Testing/12-` through `14-phase3-*-test-report.md` for live/automated verification evidence. | Remediation Program — Phase 3 |
| 2026-07-12 | Phase 4, Workflow Group 1 (Employee Management): 7 new findings added (KOM-071 through KOM-077), all 7 now Fixed. 5 fixed and live-verified immediately (KOM-071 — critical, 3 workflows fatally broken by a missing `personal_email` column; KOM-074 — one-directional account disable; KOM-075 — exit date not enforced; KOM-076 — no rehire duplicate detection; KOM-077 — incomplete audit log). 2 initially flagged as business-policy decisions rather than fixed unilaterally (KOM-072 — promotion/transfer/termination bypass the approval engine; KOM-073 — unrestricted status transitions) — user reviewed and directed a full fix of both, now implemented and live-verified (termination/transfer/promotion wired into the approval engine; a status-transition matrix enforced with a super_admin override). Register total grew from 70 to 77 findings. See Change Control Log and `Workflows/01-employee-management-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 2 (Department & Position Management): 3 new findings added (KOM-078 through KOM-080), all fixed and live-verified same round (KOM-078 — critical, `positions` had no management UI anywhere and neither `departments` nor `positions` had seed data; KOM-079 — duplicate department name crashed the Settings page; KOM-080 — department/position disable had no deletion-protection visibility). Register total grew from 77 to 80 findings. See Change Control Log and `Workflows/02-department-position-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 3 (Leave Management): 3 new findings added (KOM-081 through KOM-083); KOM-007 (open since Phase 0) also closed. KOM-081 (critical — rejected leave never restored `remaining_days`) and KOM-082 (`approval_workflows` never reflected the real leave decision) fixed and live-verified; KOM-007 (`notifyRole()` crashed every leave submission) fixed — correctly out of scope for Phases 1–3, addressed here. KOM-083 (leave approval is single-stage HR-only, not two-stage as modeled) left Open, flagged for a product decision. Register total grew from 80 to 83 findings. See Change Control Log and `Workflows/03-leave-management-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 4 (Attendance & Timesheets): 1 new finding added (KOM-084); KOM-003 (open since Phase 0) also closed. KOM-003 (critical — kiosk allowed remote clock-in impersonation with no location binding) fixed and live-verified — correctly out of scope for Phase 1, addressed here. KOM-084 (overtime approval had no duplicate-action guard) fixed and live-verified. Two completeness gaps documented but not built (payroll never reads attendance/overtime data; no automated holiday calendar). Register total grew from 83 to 84 findings. See Change Control Log and `Workflows/04-attendance-timesheets-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 5 (Payroll): 2 new findings added (KOM-085, KOM-086); KOM-030 (open since Phase 0) also closed with a genuine 5-way concurrent-request test (only 1 of 5 simultaneous publish requests won the atomic race). KOM-085 (deductions/savings never reflected in payslip totals) and KOM-086 (32 orphaned `payroll_deductions` rows) both left Open — the former changes real pay calculations, the latter involves deleting existing rows, both correctly flagged for explicit decision rather than acted on unilaterally. Register total grew from 84 to 86 findings. See Change Control Log and `Workflows/05-payroll-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | KOM-085/KOM-086 user decisions applied: KOM-085 accepted as designed (no code change); KOM-086's 32 orphaned rows backed up then deleted, 0 orphans remain. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 6 (Performance Management): 0 new findings; KOM-049 (open since Phase 0, originally Low severity) closed with a severity correction to Critical — investigation revealed the real bug was 3 nonexistent columns causing a 100%-failure fatal crash on every review-creation attempt, not the single-field mismatch originally reported. One completeness gap documented but not built (no employee acknowledgement step, no dedicated KPI mechanism). Register total unchanged at 86 (severity reclassification, not a new finding). See Change Control Log and `Workflows/06-performance-management-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 7 (Recruitment): 2 new findings added (KOM-087, KOM-088). KOM-087 (critical — no way to create a recruitment application existed anywhere; only demo seed data ever populated the table) fixed and live-verified with a new "Add Application" entry point, mirroring the existing "Post Vacancy" pattern. KOM-088 (no employee-conversion step, despite a `converted_to_employee_id` column existing in the schema) left Open, flagged for a product decision — a genuine feature build across two systems, not a bug fix. Register total grew from 86 to 88 findings. See Change Control Log and `Workflows/07-recruitment-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | KOM-088 user decision applied: deferred, left as a manual disconnected step, no code change. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 8 (Training): 0 new findings; KOM-008 (open since Phase 0, originally High severity, reported as a single column mismatch) closed with a severity correction to Critical — investigation revealed the entire Training module was fatally broken in 3 separate places (enrol, view attendance, add program) plus 2 further silent display bugs, not the 1 mismatch originally reported. Added the previously entirely-missing "Mark Attended" completion step. Register total unchanged at 88 (severity reclassification, not a new finding). See Change Control Log and `Workflows/08-training-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-12 | Phase 4, Workflow Group 9 (Consultant Module): 1 new finding added (KOM-089 — consultant deletion had no server-side impact preview or type-to-confirm safeguard, unlike the established `employees/delete.php` pattern, despite cascading to all `consultant_attendance`/`consultant_scopes` history) fixed and live-verified — `delete.php` rewritten to a GET confirmation page + type-the-consultant-number-to-confirm flow. The stale Phase 2 regression test (written against the old instant-delete behavior) was updated to match; re-ran 29/29. No connection between consultant attendance and any payment/invoicing process was found — documented as informational, not treated as a defect (contractors are commonly paid outside a payroll system built for employees). Register total grew from 88 to 89 findings. See Change Control Log and `Workflows/09-consultant-module-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-13 | Phase 4, Workflow Group 10 (Temporary Employee Module): 3 new findings added (KOM-090 through KOM-092); KOM-020 (open since Phase 0/Audit II) also closed. KOM-020 (reflected XSS via `$_GET['status']` in the export status banner) fixed and live-verified — correctly out of scope for Phase 2, addressed here. KOM-091 (temp employee deletion had no safety confirmation) fixed with the same type-to-confirm pattern as KOM-089. KOM-092 (`auditLog()` called with the wrong argument order in `add.php`/`edit.php`/`delete.php`, corrupting `record_id`/`old_value` for the module's entire audit trail) fixed. KOM-090 (Kiosk/Timesheet attendance-method UI claimed working clock-in capture that doesn't exist for temp employees) partially fixed per user decision — misleading copy corrected, underlying capture mechanism deferred to a future phase. Register total grew from 89 to 92 findings. See Change Control Log and `Workflows/10-temp-employee-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-13 | Phase 4, Workflow Group 11 (Notifications): 3 new findings added (KOM-093 through KOM-095), all fixed and live-verified. KOM-093 (critical — the shared notification-dropdown renderer inserted `title`/`message` into `innerHTML` with no escaping anywhere in the chain, and the employee-portal Hub fed a low-privilege employee's raw free text into a notification sent to every `hr_manager`/`super_admin`, a genuine stored-XSS privilege-escalation chain) fixed by escaping at the shared rendering chokepoint in `footer.php`. KOM-095 (`ApprovalEngine::act()` never notified the request's initiator of an approve/reject decision, for any of the three workflow types actually in use) fixed with a new `notifyInitiator()` method, generalizing leave's own existing notify-on-decision pattern to the whole engine. KOM-094 (the same invalid-`notifications.type`-ENUM bug class self-corrected in Workflow Group 1, found still live in `employee-portal/hub.php`) fixed. Training, Recruitment, and password-reset notifications, and any scheduled/reminder mechanism, confirmed absent entirely (no cron/scheduler exists in the codebase) — documented as completeness gaps, not built. Register total grew from 92 to 95 findings. See Change Control Log and `Workflows/11-notifications-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-13 | Phase 4, Workflow Group 12 (Documents Generation Lifecycle): 2 new findings added (KOM-096, KOM-097); KOM-006 and KOM-022 (both open since the baseline audit) also closed. KOM-006 (critical — every branding-asset `.htaccess` blocked all letterhead/signature/stamp/watermark image serving, confirmed live via direct HTTP request — the entire document-branding feature was non-functional) fixed by rewriting all 4 `.htaccess` files to the same correctly-scoped syntax already used by the parent `uploads/.htaccess`; confirmed images now serve (200) while script execution stays blocked (403). KOM-022 (high — template bodies were unsanitized, a stored-XSS-via-template-authoring gap) fixed with a new DOMDocument-based `DocumentEngine::sanitizeTemplateHtml()` applied at save time; a libxml quirk that would have broken `{{placeholder}}`s inside `href`/`src` attributes was found during testing and fixed with a protect/restore token swap. KOM-096 (a broken operator-precedence check meant the "Documents Expiring Soon" banner never showed a separator between entries) fixed. KOM-097 (the QR-code feature on generated documents links to a verification page that doesn't exist anywhere) left deferred per user decision — dormant, 0 of 47 live templates currently enable it. Register total grew from 95 to 97 findings. See Change Control Log and `Workflows/12-documents-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-13 | Phase 4, Workflow Group 13 (Reports & Dashboards Consistency): 2 new findings added (KOM-098, KOM-099), both addressed. KOM-098 (high — `attendance.is_absent` defaults to 0 and is never written by any code path anywhere in the codebase, so every "Absent" figure on the Dashboard and in Reports has been structurally guaranteed to show 0, permanently, since the column's introduction) partially fixed — the daily instances (Dashboard's "Absent Today"/7-day trend, the Attendance module's day summary) recomputed as active employees minus those with an attendance record for that date, live-verified against real data; the monthly/period instances (Reports' attendance report, executive KPIs, timesheet report) left documented, not fixed, since a correct multi-day figure needs a working-day/holiday calendar this codebase does not have. KOM-099 (the Dashboard's "Pending Approvals" header total silently disagreed with its own 5 visible rows, omitting recruitment applications) fixed. Register total grew from 97 to 99 findings. See Change Control Log and `Workflows/13-reports-dashboards-workflow-report.md` for full evidence. | Remediation Program — Phase 4 |
| 2026-07-13 | Phase 4 close-out: KOM-045 (unused permission slugs, deferred from Phase 3) re-verified fresh against the post-Workflow-Group-13 codebase — count corrected from 24 to 26 (a few slugs originally flagged unused were incidentally wired up by Phase 1–3 fixes; a few new never-wired slugs were seeded in the meantime). Closed per explicit user decision: reviewed and accepted as a documented, reserved set (8 redundant export slugs, 5 portal.\* feature-toggle slugs, 13 miscellaneous), no code or database changes made. No register-total change — a status update to an existing finding. All 13 Phase 4 workflow groups now complete; see the Phase 4 Completion Report for the full program summary. | Remediation Program — Phase 4 |
| 2026-07-13 | Phase 5 begins: full re-audit of all 99 register rows found the "Open" tally drifted from 31 to a true 28 (corrected to 27, then 26 after this round); KOM-035 closed as a stale duplicate of the already-fixed KOM-092. Stage 5.1 (Leave Approval Model): KOM-083 resolved — locked in single-stage HR-only leave approval as the permanent design per user decision, removing the never-enforced Supervisor Review stage from `ApprovalEngine::workflowConfig()`. Live-verified with a real leave application end to end (1 stage created, HR approval flips application/workflow/stage together). No register-total change — both are status updates to existing findings. See `Phase5/01-phase5-open-findings-scope.md` and `Phase5/02-leave-approval-completion-report.md` for full evidence. | Remediation Program — Phase 5 |
| 2026-07-13 | Phase 5, Stage 5.2 (ApprovalEngine Dormant Workflow Types): KOM-047 fixed (`cancel()`'s `+`-vs-concatenation bug); 4 never-instantiated workflow types (`overtime`, `correction`, `payroll_run`, `document`) removed from `ApprovalEngine::workflowConfig()` per user decision, since each already has its own working, independent approval mechanism. Live-verified: Approvals UI type filter now lists exactly the 4 real types; a full termination-workflow round trip re-confirmed correct after the config change. No register-total change — a status update to an existing finding. See `Phase5/03-approvalengine-workflow-resolution-report.md` for full evidence. | Remediation Program — Phase 5 |
| 2026-07-13 | Phase 5, Stage 5.3 (Working-Day & Holiday Calendar): completes KOM-098's deferred monthly/period half. New `work_calendar_settings`/`work_calendar_holidays` tables and 5 calendar functions built and unit-tested (17/17); wired into Dashboard's Monthly Attendance Rate, `modules/reports/{index,executive,timesheets}.php`'s absence figures (now correctly include never-clocked-in employees instead of silently excluding them), `leave/apply.php`'s day-count calculation (now holiday-aware, previously hardcoded weekend-only), and the daily Absent figures (now 0 on a non-working day). New admin UI at `modules/settings/calendar.php`. One test-cleanup batch error disclosed and corrected within the same verification pass (see Phase5/04 report §4). No register-total change — a status update to an existing finding. See `Phase5/04-working-day-calendar-report.md` for full evidence. | Remediation Program — Phase 5 |
| 2026-07-13 | Phase 5, Stage 5.4 (Scheduled Task Infrastructure): new `cron/` scheduler (run.php, bootstrap.php, 4 task files, `scheduled_task_locks`/`scheduled_task_runs` tables) — no cron mechanism existed anywhere in this codebase before now. Live-verified: web access blocked at 2 independent layers (403); CLI run processes all 4 tasks; overlapping-run lock correctly rejects a second invocation; stale-lock auto-recovery after a simulated crash; failure isolation confirmed with a forced task failure not affecting the other 3; both functional tasks (`expire_tokens`, `cleanup_safe_temp_files`) correctly process and are idempotent against real disposable test data. No specific finding — new infrastructure for later stages. No register-total change. See `Phase5/05-scheduler-infrastructure-report.md` for full evidence. | Remediation Program — Phase 5 |
| 2026-07-13 | Phase 5, Stage 5.5 (Self-Service Password Recovery, Admin Surface Only): KOM-041 fixed. Built forgot-password → emailed one-time token (sha256-hashed, 1-hour expiry, single-use) → reset flow for the Admin surface, enumeration-resistant and rate-limited; `users.password_changed_at` compared against each session's own `login_time` in `auth/session.php` to force re-login on other active sessions after any password change/reset. Live-verified end-to-end with a genuine two-independent-session test: an already-authenticated session (200 OK) was confirmed forced to a 302 redirect to `login.php?reason=password_changed` immediately after a second, independently-triggered reset completed for the same account. A minor self-introduced `email_logs.type` enum gap (missing `'password_reset'`, silently coerced to `''` under non-strict SQL mode) was caught during live testing and fixed the same stage — disclosed in full in the stage report. Employee/Consultant/Temp Portal remain admin-assisted-only, per the Stage 5.5 decision matrix. Register total unchanged at 99 (status update to an existing finding). See `Phase5/06-password-recovery-report.md` for full evidence. | Remediation Program — Phase 5 |
| 2026-07-13 | Phase 5, Stage 5.6 (Deferred Notification Workflows): filled in `cron/tasks/send_reminders.php` with 9 reminder categories (contract/probation/temp-employee/consultant expiry, document expiry, training starting soon, recruitment interview tomorrow, leave approval unactioned, payroll finalized-not-published) — addresses the Training/Recruitment/scheduled-reminder gaps documented in Phase 4 Workflow Group 11. A design gap (day-threshold-only matching would repeat on every run at the scheduler's own documented 15-30 minute recommended cadence) was found and fixed during live testing with a new `reminder_notifications_log` per-day dedup table. Live-verified: a 9-category test fired every category exactly once; an immediate second run produced zero duplicates. No specific finding — new infrastructure, same treatment as Stage 5.4. No register-total change. See `Phase5/07-deferred-notifications-report.md` for full evidence. | Remediation Program — Phase 5 |
| 2026-07-13 | Phase 5, Stage 5.7 (Recruitment-to-Employee Conversion): KOM-088 fixed. Built a guided conversion — a "Convert to Employee" action on a `selected` application pre-fills the existing Add Employee form (`?from_application=<id>`) and links `converted_to_employee_id` on save, re-validated at POST time against the same convertible-state condition to prevent double-conversion. Live-verified: pre-fill, full conversion, re-conversion-blocked, and baseline-flow-unaffected cases all confirmed. Register total unchanged at 99 (status update to an existing finding). See `Phase5/08-recruitment-conversion-report.md` for full evidence. | Remediation Program — Phase 5 |
| 2026-07-13 | Phase 5, Stage 5.8 (Temporary Employee Attendance Capture): KOM-090 and KOM-058 fixed. Built supervisor/HR-entered digital attendance capture (new `temp_attendance` table + `attendance_entry.php`, reusing the existing project/site/week selector), per user decision, in place of a kiosk-based path. Live-verified with real data: entry, pre-fill on reload, `view.php` summary total, and audit trail all confirmed. Register total unchanged at 99 (status updates to existing findings). See `Phase5/09-temp-attendance-capture-report.md` for full evidence. | Remediation Program — Phase 5 |
