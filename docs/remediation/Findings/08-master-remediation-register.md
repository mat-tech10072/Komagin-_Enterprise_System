# Komagin HR — Master Remediation Register

**Document type:** Phase 0 Baseline Deliverable #8 of 9
**Status:** Documentation only — this register catalogues existing findings. No finding in this document has been fixed, patched, or otherwise remediated as part of producing it.
**Date compiled:** 2026-07-11
**Baseline tag:** `v1.0-enterprise-baseline`

---

## Source Documents

This register consolidates findings from three sources into one deduplicated list, each finding appearing exactly once:

1. **Audit I** — `Komagin_HR_Audit_Report_2026-07-10.html` (baseline independent audit, 34 findings: C-01–04, H-01–12, M-01–09, L-01–09)
2. **Audit II** — `Komagin_HR_Audit_Followup_2026-07-10.html` (same-day follow-up: regression-check of Audit I + 21 new findings: NC-01–02, NH-01–05, NM-01–07, NL-01–07)
3. **Phase 0 baseline inventory** (this program, 2026-07-11) — 12 newly discovered items surfaced while documenting the system, not previously reported by either audit

Where a finding was reported independently by more than one source (e.g. document-generation permission gating, reported as both `H-06` and `NM-04`), the two source IDs are merged into a single register entry with one final severity assessment, noted in the **Source ID(s)** column.

## Uniform Fields (apply to every finding unless a row overrides them)

Per Phase 0 rules, **no finding has been remediated yet.** Unless a row states otherwise:
- **Completion Status:** Not Started
- **Verification Status:** Not Verified
- **Date Closed:** — (none closed)
- **Regression Test Required:** Yes — see `Testing/09-baseline-test-plan.md` for the per-module test case that must pass before this finding can be marked Verified

## Target Phase Key

| Phase | Scope |
|---|---|
| **Phase 1** | Critical findings — live authorization bypasses, broken live features, DR/schema integrity |
| **Phase 2** | High findings — data exposure, privilege escalation, silent data-integrity failures |
| **Phase 3** | Medium findings — narrower-impact defects, policy/permission-granularity inconsistencies |
| **Phase 4** | Low findings — latent risk, dead code, hygiene, completeness gaps |
| **Phase 5** | Process/governance changes (not a single code fix — see Verification Report §4) |

---

## CRITICAL (6)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase | Verification Required |
|---|---|---|---|---|---|---|---|---|
| KOM-001 | C-01 | Approval engine has no approver-role authority check | Approvals | `modules/approvals/index.php`, `config/ApprovalEngine.php` | `index.php` gates with `requireLogin()` only, never `requirePermission()`; `ApprovalEngine::act()` never compares the acting user's role to the current stage's `approver_role` — any authenticated user can approve/reject any workflow. | Open (reconfirmed by regression check, Audit II) | Phase 1 | Code review + functional test: attempt approval as a non-approver role, confirm rejection |
| KOM-002 | C-02 | Consultants module calls undefined CSRF function | Consultants | `modules/consultants/{add,edit,delete,scope_save}.php` | All four call `validateCsrfToken()`, which is never defined anywhere (correct helper is `verifyCsrfToken()`); every write throws a PHP Fatal Error. | Open (reconfirmed) | Phase 1 | Functional test: submit each of the 4 forms, confirm no fatal error |
| KOM-003 | C-03 | Kiosk allows remote clock-in impersonation | Attendance/Kiosk | `modules/attendance/kiosk.php` | With no `?t=` kiosk token, falls back to any globally-open kiosk session; no login, no CSRF, employee numbers are sequential/guessable; rate-limit only counts invalid-number attempts. | Open (reconfirmed) | Phase 1 | Security test: POST from an external client with a guessed valid employee number, confirm rejection without a valid location-bound token |
| KOM-004 | C-04 + Phase 0 DB inventory | Tracked SQL files cannot reconstruct a working database — 12 tables undefined, 1 broken forward-reference | Database / Schema | `database/schema.sql` and all `phase*.sql` files | 12 tables used by live code (`doc_templates`, `doc_categories`, `generated_documents`, `doc_template_versions`, `kiosk_sessions`, `kiosk_audit`, `employee_dependents`, `employee_qualifications`, `employee_work_history`, `approval_workflows`, `approval_stages`, `settings`) have no `CREATE TABLE` anywhere in the repo; `phase5_branding_theme.sql` ALTERs `doc_templates`, which no file ever creates — the tracked migration chain cannot be run end-to-end against an empty database. | Open — **scope expanded** from 4 tables (Audit I) to 12 (Phase 0 inventory) | Phase 1 | DBA task: attempt a from-empty install using only tracked `.sql` files in documented order; capture every failure |
| KOM-005 | NC-01 | temp_employees writes to schema-undefined columns | Temp Employees | `modules/temp_employees/{add,edit}.php`, `database/phase8_temp_employees.sql` | `add.php`/`edit.php` INSERT/UPDATE `rate_type` and `attendance_method`, neither defined in `phase8_temp_employees.sql`. | Open | Phase 1 | DBA task: diff live DB `DESCRIBE temp_employees` against `phase8_temp_employees.sql`; determine which side is authoritative |
| KOM-006 | NC-02 | Branding asset folders' `.htaccess` likely blocks all letterhead/signature/stamp/watermark image serving | Documents / Branding | `uploads/{letterheads,signatures,stamps,watermarks}/.htaccess` | Legacy Apache 2.2 `Deny from all` syntax, inconsistent with the parent `uploads/.htaccess`'s modern, narrowly-scoped syntax; XAMPP's default Apache 2.4 loads `mod_access_compat`, which keeps this legacy directive functional — very likely blocking every branded image. | Open — **not yet live-verified** | Phase 1 (verify first, fix if confirmed) | **Live HTTP check**: request a known branding image URL directly in a browser; if 403, confirmed |

---

## HIGH (18)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase | Verification Required |
|---|---|---|---|---|---|---|---|---|
| KOM-007 | H-01 | Leave application crashes after it's already been saved | Leave | `modules/leave/apply.php` | `notifyRole()` called with an array where a single string role is required (also missing an argument vs. current signature) — throws after the `INSERT` has committed. | Open (reconfirmed, "worse" — also missing an argument) | Phase 2 | Functional test: submit a leave application, confirm no fatal error and HR is notified |
| KOM-008 | H-02 | Training enrolments invisible due to column-name mismatch | Training | `modules/training/{enrol,index}.php` | `enrol.php` writes `training_attendance.program_id`; `index.php` reads/joins `training_id`. | Open (reconfirmed) | Phase 2 | Functional test: enrol an employee, confirm they appear in the roster/attendee count |
| KOM-009 | H-03 | Leave approve/reject buttons non-functional | Leave | `modules/leave/{view,approve}.php` | `view.php` POSTs hidden fields; `approve.php` only reads `$_GET` — button clicks silently no-op. | Open (reconfirmed) | Phase 2 | Functional test: click Approve from the detail page, confirm status changes |
| KOM-010 | H-04 | Timesheet/overtime approval checks view permission, not approve | Timesheets | `modules/timesheets/{approve,overtime}.php` | `requirePermission('timesheets.approve')` called with no action argument, defaults to `'view'`. | **Partially changed, still exploitable** — `approve.php`'s lock/unlock branches now correctly check `'approve'`; the approve action itself and all of `overtime.php` still don't | Phase 2 | Functional test: attempt approval as a view-only role, confirm rejection |
| KOM-011 | H-05 | Executive Analytics exposes company-wide payroll totals without salary-data permission check | Reports | `modules/reports/executive.php` | Requires only `reports.view`; never calls `canViewSalaryData()`/`maskSalary()` before rendering aggregate payroll totals/CSV export. | Open — **not re-verified in Audit II, recommend re-check in Phase 2 kickoff** | Phase 2 | Code review + functional test as a non-payroll `reports.view` role |
| KOM-012 | H-07 | Consultant portal login has no session-ID regeneration (session fixation) | Consultant Portal | `consultant-portal/login.php` | No `session_regenerate_id()` call anywhere in the file, confirmed by full-text search. | Open (reconfirmed) | Phase 2 | Code review + security test: pre-set a session ID, log in, confirm ID changes |
| KOM-013 | H-08 | No CSRF protection on consultant self-service actions | Consultant Portal | `consultant-portal/{kiosk,scope}.php` | Clock in/out/break and scope-notes save have no CSRF token on form or handler. | Open | Phase 2 | Functional/security test: forged cross-site POST, confirm rejection after fix |
| KOM-014 | H-09 | Payroll deduction/savings delete bypasses the permission matrix's delete restriction | Payroll | `modules/payroll/{deductions,savings}.php` | `requirePermission()` called with no action (defaults to view); delete branches inherit that gate despite the matrix setting `can_delete=0` for payroll roles. | Open (reconfirmed) | Phase 2 | Functional test: attempt delete as a role with `can_delete=0`, confirm rejection |
| KOM-015 | H-10 | User role assignment has no server-side allow-list — privilege escalation | Users | `modules/users/index.php` | `$_POST['role']` inserted after only a non-empty check; an HR Manager can POST `role=super_admin`. | Open (reconfirmed) | Phase 2 | Security test: attempt to set an unauthorized role value via direct POST, confirm rejection |
| KOM-016 | H-11 | Published payslips editable with no audit trail | Payroll | `modules/payroll/payslips.php` | Update branch has no status guard (can edit `finalized`/`sent` payslips) and never calls `auditLog()`. | Open (reconfirmed) | Phase 2 | Functional test: edit a published payslip, confirm rejection or that an audit entry is recorded |
| KOM-017 | H-12 | Employee portal login has no session-ID regeneration (session fixation) | Employee Portal | `employee-portal/login.php` | Neither the permanent nor temp-employee branch calls `session_regenerate_id()`. | Open (reconfirmed) | Phase 2 | Same as KOM-012, employee-portal surface |
| KOM-018 | NH-01 | Dashboard leaks raw audit-log data to every logged-in user regardless of role | Dashboard | `dashboard.php` | Page gated by `requireLogin()` only; "Recent Activity" widget runs `SELECT * FROM audit_logs LIMIT 8` and renders it unconditionally — no `audit.view`/`activity_log` permission check on this specific widget. | Open | Phase 2 | Functional test: view dashboard as a narrowly-scoped role (e.g. `kiosk_terminal`), confirm widget is absent or filtered |
| KOM-019 | NH-02 | Activity Log module bypasses the permission system entirely | Activity Log | `modules/activity_log/{index,user,download}.php`, `includes/header.php` | Hardcodes `$_SESSION['user_role'] !== 'super_admin'` instead of `requirePermission()`; not controllable via the Roles & Permissions UI at all. | Open | Phase 2 | Code review: confirm module is wired through `requirePermission()` with a seeded slug post-fix |
| KOM-020 | NH-03 | Reflected XSS in temp_employees export status banner | Temp Employees | `modules/temp_employees/index.php` | `$_GET['status']` echoed unescaped at line 104, while the adjacent search-term display correctly escapes. | Open | Phase 2 | Security test: submit `status=<script>` payload, confirm it renders escaped post-fix |
| KOM-021 | NH-04 | Any documents.view holder can view/print any employee's generated document by guessing an ID (IDOR) | Documents | `modules/documents/view_generated.php` | Fetches any `generated_documents` row by `id` with only the blanket `documents.view` check — no per-employee scoping, no audit entry on view. | Open | Phase 2 | Security test: attempt to view another employee's generated document by ID enumeration |
| KOM-022 | NH-05 | Document template body is unsanitized — stored XSS via template authoring | Documents | `modules/documents/templates.php`, `config/DocumentEngine.php` | `render()` only escapes `{{placeholder}}` substitutions, not the surrounding template markup; a role with template create/edit rights can embed `<script>` that executes for every future viewer, including more-privileged approvers. | Open | Phase 2 | Security test: save a template containing a script payload, confirm it's neutralized post-fix |
| KOM-023 | Phase 0 — new | `hrofficer`/`hr_officer` role-name typo silently denies two modules to a legitimate role | Permissions / Temp Employees / Consultants | `database/phase8_temp_employees.sql`, `database/phase9_consultants.sql` | Both files seed permissions to role string `'hrofficer'` (no underscore); the canonical ENUM value everywhere else is `'hr_officer'`. Real `hr_officer` users get zero `role_permissions` rows for `temp_employees.*`/`consultants.*` — a functional access-denial bug affecting every HR Officer, not a security exposure. | Open — newly discovered in Phase 0 baseline | Phase 2 | Functional test: log in as `hr_officer`, confirm access to Temp Employees and Consultants modules matches intended design |
| KOM-024 | Phase 0 — new | Fresh installation is missing all phase 1–9 tables, permissions, and templates | Deployment / Database | `database/install.php` | Installer executes `schema.sql` only; on-screen instructions mention `migration_v2.sql` but never phase1/5/6/7/8/9 — a fresh install following only the installer's own guidance ends up without the permission matrix, branding tables, 47 document templates, or the temp-employee/consultant modules' schema. | Open — newly discovered in Phase 0 baseline | Phase 2 | Deployment test: run `install.php` on an empty DB, confirm which modules are non-functional immediately after |

---

## MEDIUM (21)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase | Verification Required |
|---|---|---|---|---|---|---|---|---|
| KOM-025 | M-01 | Employee salary field always blank on edit | Employees | `modules/employees/edit.php` | Reads `$emp['salary']`; canonical/actual column is `basic_salary`. | Open — not re-verified in Audit II | Phase 3 | Functional test: open edit form for an employee with a salary set, confirm field is pre-populated |
| KOM-026 | M-03 | Notification "mark read" endpoints are CSRF-able via plain GET | Notifications API | `api/notifications.php` | `mark_read`/`mark_all_read` mutate state on GET with only a login check, no token. | Open | Phase 3 | Security test: forged cross-origin GET, confirm rejection post-fix |
| KOM-027 | M-04 | Employee portal hub request submission has no CSRF token | Employee Portal | `employee-portal/hub.php` | Unlike the HR-facing counterpart, which does verify CSRF. | Open | Phase 3 | Security test: forged cross-site POST, confirm rejection post-fix |
| KOM-028 | M-05 | Reports Hub "Export CSV" link is dead | Reports | `modules/reports/index.php` | Nothing reads `$_GET['export']`, unlike employees/timesheets/executive reports where it works. | Open | Phase 3 | Functional test: click Export CSV, confirm a file downloads |
| KOM-029 | M-06 | Temp-employee portal has weaker session lifecycle than the main portal | Employee Portal (temp) | `employee-portal/temp_portal.php` | Doesn't include the shared `_session.php`; no 30-min ID rotation, no true inactivity timeout, relies only on the 8h absolute cookie. | Open | Phase 3 | Code review: confirm temp portal session handling matches main portal pattern post-fix |
| KOM-030 | M-07 | Payroll run publish/finalize has a check-then-act race condition | Payroll | `modules/payroll/{run_publish,run_finalize}.php` | `SELECT` status check followed by `UPDATE`, no transaction/row lock — concurrent submissions can both pass the check, risking duplicate payslip emails. | Open | Phase 3 | Concurrency test: two simultaneous publish requests, confirm only one set of emails sent post-fix |
| KOM-031 | M-08 | SMTP password exposed in page HTML source | Settings | `modules/settings/email.php` | Second "Payslip Notifications" form re-emits `smtp_pass` as a hidden input's cleartext `value` attribute (primary form now correctly uses a placeholder). | **Partially changed, still exploitable** — primary form fixed, second form still leaks | Phase 3 | Manual check: View Source on the settings page, confirm no cleartext password anywhere post-fix |
| KOM-032 | M-09 | One permission grants CRUD over four unrelated branding asset types | Settings / Branding | `modules/settings/branding.php` | Gates letterheads/signatures/stamps/watermarks (incl. create/edit/delete) behind a single `branding.letterheads` check, despite 4 separate permission slugs existing in seed data. | Open | Phase 3 | Code review: confirm each asset type is gated by its own slug post-fix |
| KOM-033 | NM-01 | Activity Log CSV export doesn't neutralize spreadsheet formula injection | Activity Log | `modules/activity_log/download.php` | Escapes double-quotes but not leading `=`,`+`,`-`,`@` in free-text exported fields (reason, old_value, new_value). | Open | Phase 3 | Security test: export a record containing a formula-injection payload, confirm neutralization post-fix |
| KOM-034 | NM-02 | Activity Log per-user export ignores filters and loads unbounded data | Activity Log | `modules/activity_log/download.php` | Per-user export path ignores date-range filters respected elsewhere in the same file; uses `fetchAll()` instead of the streaming pattern used by category exports. | Open | Phase 3 | Functional test: export a long-tenured user's history with a date filter applied, confirm the filter is respected post-fix |
| KOM-035 | NM-03 | Temp Employees audit trail logs the wrong record ID | Temp Employees | `modules/temp_employees/{add,edit,delete}.php` | `auditLog()` called with the acting admin's user ID in the record-ID argument, instead of the affected temp employee's ID. | Open | Phase 3 | Functional test: create/edit/delete a temp employee, confirm the resulting audit_logs row's record_id matches the temp employee, not the admin |
| KOM-036 | H-06 + NM-04 | Document generation save action does not re-check create-level permission | Documents | `modules/documents/generate.php` | Whole page gated by `documents.view`; the `save` action (which persists a new document containing PII) never separately checks a create/upload-level permission, unlike `templates.php`. Not exploitable under current seeded roles (view+create are bundled for every role holding either), but a genuine code-level gap. | Open — reported independently by both audits, merged here | Phase 3 | Code review + functional test once a view-only role exists in the permission matrix |
| KOM-037 | NM-05 | Two menu items, two authorization models, one underlying audit table | Navigation | `includes/header.php`, `modules/audit/index.php`, `modules/activity_log/*` | "Audit Logs" (permission-gated via `audit.view`) and "Activity Logs" (hardcoded super-admin-only) sit adjacent in the sidebar, both reading `audit_logs`. | Open | Phase 3 | Product decision + code review: confirm a single, consistently-gated entry point post-fix |
| KOM-038 | NM-06 | SVG accepted for letterhead uploads | Settings / Branding | `modules/settings/branding.php` | `image/svg+xml` allowed for letterheads only; SVG can carry embedded script; server-side MIME sniffing will pass a well-formed malicious SVG. Not currently exploitable (rendered only via `<img src>`), but stacks with KOM-006/KOM-039. | Open | Phase 3 | Code review: confirm SVG is excluded or sanitized post-fix |
| KOM-039 | NM-07 | Uploaded branding file extension trusted from client filename | Settings / Branding | `config/functions.php` (`uploadFile()`) | Extension saved to disk comes from the client-supplied original filename, not cross-checked against the MIME type detected by `finfo`. | Open | Phase 3 | Security test: upload a polyglot file with a mismatched extension, confirm rejection post-fix |
| KOM-040 | Phase 0 — new (consolidates M-02) | Hardcoded role-list authorization checks bypass the DB-driven permission system (13 occurrences, 12 files) | System-wide | `modules/timesheets/corrections.php:17`, `modules/employees/view.php:769`, `modules/assets/index.php:85`, `modules/documents/index.php:137`, `modules/leave/{view.php:35,apply.php:17,index.php:147}`, `modules/archive/monthly.php:155`, `modules/temp_employees/{view.php:278,add.php:263,edit.php:273}`, `modules/activity_log/{index.php:26,download.php:215}` | Each uses `in_array($_SESSION['user_role'], [hardcoded list])` instead of `requirePermission()`/`canX()`. A custom role granted equivalent rights through the Roles & Permissions UI is silently denied at these 13 specific checks regardless of what the matrix says. Absorbs and expands the scope of previously-reported finding M-02 (`timesheets/corrections.php`), now documented as one system-wide pattern rather than a single-file instance. | Open — newly consolidated in Phase 0 baseline | Phase 3 | Code review: confirm each of the 13 sites is migrated to the standard permission functions |
| KOM-041 | Phase 0 — new | No self-service password-reset flow exists on any of the 4 authentication surfaces | Authentication (all surfaces) | `auth/login.php`, `employee-portal/login.php`, `consultant-portal/login.php` | The only recovery path is an already-authenticated admin manually resetting another admin-surface user's password; employee/consultant portals display "contact HR" text with no link or mechanism at all. This is a designed limitation, not a code defect — documented here as an operational gap for remediation planning to explicitly accept or address. | Open — newly discovered in Phase 0 baseline | Phase 3 | Product decision: confirm whether this is accepted as intended, or scheduled for a self-service reset feature |
| KOM-042 | Phase 0 — new | Employee/Consultant/Self-Service portal session cookies never set the Secure flag under any condition | Employee Portal / Consultant Portal / Self-Service | `employee-portal/{login,_session}.php`, `consultant-portal/{login,_session}.php`, `self-service/update.php` | Only the admin surface (`auth/session.php`) conditionally sets `session.cookie_secure` based on HTTPS/`APP_ENV`; the other three surfaces set cookie params inline and never consider HTTPS at all. | Open — newly discovered in Phase 0 baseline | Phase 3 | Code review: confirm all 4 surfaces share one HTTPS-aware cookie configuration post-fix |
| KOM-043 | Phase 0 — new | Consultant portal logout does not destroy the PHP session | Consultant Portal | `consultant-portal/logout.php` | Neither `session_unset()` nor `session_destroy()` is called — only 7 named `cp_*` session keys are individually unset; the underlying session ID/cookie remains valid on the client, with any key outside that specific list surviving "logout." | Open — newly discovered in Phase 0 baseline | Phase 3 | Functional test: log out, inspect session state server-side, confirm full destruction post-fix |
| KOM-044 | Phase 0 — new | Executive Analytics payroll-exposure finding needs re-verification | Reports | `modules/reports/executive.php` | Carried forward from KOM-011 (H-05) as an explicit action item — Audit II's regression check did not re-read this file; status should not be assumed unchanged without a fresh look. | Not re-verified | Phase 2 (paired with KOM-011) | Re-run the original verification method against current code before Phase 2 planning finalizes |
| KOM-045 | Phase 0 — new | 24 seeded permission slugs are never referenced anywhere in application code | Permissions | `database/{phase1_permissions,phase5_branding_theme}.sql` | Includes `dashboard.view`, `dashboard.analytics`, `employees.export`, `attendance.edit/approve/export`, `leave.apply/export`, `recruitment.export`, `training.export`, `assets.assign`, `documents.delete`, `archive.lock`, `hub.manage`, `portal.*` (5 slugs), `users.view`, `audit.export`, `branding.signatures/stamps/watermarks`, `email.send/logs`. Not necessarily defective — may be reserved for planned features (e.g. the 3 unused branding slugs look intended to replace KOM-032's coarse gate) — flagged for an explicit keep/wire-up/remove decision. | Open — newly discovered in Phase 0 baseline | Phase 3 | Product decision, slug by slug |

---

## LOW (22)

| ID | Source ID(s) | Title | Module | Files Affected | Description | Current Status | Target Phase |
|---|---|---|---|---|---|---|---|
| KOM-046 | L-01 | Pagination values interpolated directly into SQL | Employees, Attendance, Timesheets, Recruitment, Onboarding, Training | 6 `index.php` files | Values are int-cast before use (not currently exploitable) but deviate from the prepared-statement standard used elsewhere. | Open | Phase 4 |
| KOM-047 | L-02 | Approval-cancellation reason silently corrupted | Approvals | `config/ApprovalEngine.php` (`cancel()`) | Uses `+` instead of string concatenation (MySQL numeric coercion); currently dead/unused code. | Open | Phase 4 |
| KOM-048 | L-03 | Missing-documents report runs one query per employee (N+1) | Documents | `modules/documents/missing.php` | Loops per-employee instead of a single joined query; scales linearly with headcount. | Open | Phase 4 |
| KOM-049 | L-04 | Performance review rating never saved | Performance | `modules/performance/save.php` | Reads `overall_rating`; form posts `overall_score`. | Open | Phase 4 |
| KOM-050 | L-05 | No CSRF token on consultant portal login form | Consultant Portal | `consultant-portal/login.php` | Login-CSRF risk (forces victim to authenticate as attacker-controlled account); lower severity, doesn't compromise the victim's own account. | Open | Phase 4 |
| KOM-051 | L-06 | Archive "Lock" control silently no-ops | Archive | `modules/archive/monthly.php` | Posts a `lock_id` field the handler never reads; visibility gated by hardcoded role list (see also KOM-040). | Open | Phase 4 |
| KOM-052 | L-07 | No brute-force lockout on employee-portal login | Employee Portal | `employee-portal/login.php` | No failed-attempt tracking, unlike the admin login's 5-attempt/15-min lockout. | Open | Phase 4 |
| KOM-053 | L-08 | Error display defaults to on | Config | `config/config.php` | `APP_ENV` defaults to `'development'` unless explicitly set otherwise. | Open | Phase 4 |
| KOM-054 | L-09 | Standing deployment hardening items unaddressed | Environment | `config/config.php`, `.htaccess` | Blank root DB password, default admin password, no HTTPS redirect, no CSP header. | Open | Phase 4 |
| KOM-055 | NL-01 | Replaced branding assets never deleted from disk | Settings / Branding | `modules/settings/branding.php` | No `unlink()` on update/delete; unbounded storage growth. | Open | Phase 4 |
| KOM-056 | NL-02 | Server-generated date interpolated directly into SQL | Dashboard | `dashboard.php` | Not exploitable (server-generated value), but deviates from prepared-statement standard. | Open | Phase 4 |
| KOM-057 | NL-03 | Dead query wastes a DB round-trip on every temp-employee list page load | Temp Employees | `modules/temp_employees/index.php` | Throwaway COUNT statement's `PDOStatement` object assigned then immediately overwritten. | Open | Phase 4 |
| KOM-058 | NL-04 | Temp-employee timesheet feature is a blank template, not a data-capture tool | Temp Employees | `modules/temp_employees/timesheet.php` | Renders a printable grid only; no persistence, hours calculation, or payroll linkage — not a bug, a completeness gap. | Open (informational) | Phase 4 |
| KOM-059 | NL-05 | Temp employee position modeled as free text, not FK | Temp Employees | `database/phase8_temp_employees.sql` | Inconsistent with `modules/employees`'s normalized `positions` table; complicates future cross-module reporting. | Open | Phase 4 |
| KOM-060 | NL-06 | Letterhead header/footer HTML fields saved but never used | Settings / Branding / Documents | `modules/settings/branding.php`, `config/DocumentEngine.php` | `company_letterheads.header_html`/`footer_html` captured on save, never read by the rendering engine. | Open | Phase 4 |
| KOM-061 | NL-07 | Core document tables have no traceable schema origin | Database | `doc_templates`, `doc_categories`, `generated_documents`, `doc_template_versions` | Compounds KOM-004 — these tables' true structure isn't reproducible from the repository at all. | Open (folded into KOM-004 scope, tracked separately for visibility) | Phase 1 (paired with KOM-004) |
| KOM-062 | Phase 0 — new | Self-service per-link CSRF token compared with non-constant-time `!==` | Self-Service | `self-service/update.php` | Uses plain string comparison instead of `hash_equals()`, unlike the shared `verifyCsrfToken()` helper used elsewhere. Low practical risk given the token is single-use and per-link. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-063 | Phase 0 — new | Password minimum length inconsistent across admin-reset paths | Users / Auth | `modules/users/index.php`, `auth/change_password.php` | Admin-initiated reset modal allows a 6-character minimum; self-service change requires 8. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-064 | Phase 0 — new | `requireRole()` authorization helper defined with zero call sites | Config | `config/functions.php` | Dead code — a second, parallel hardcoded-list authorization primitive that nothing in the codebase actually calls. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-065 | Phase 0 — new | `employee_skills` table defined but never used anywhere in code | Database | `database/schema.sql` | Has a `CREATE TABLE`, is never seeded or queried; `employee_qualifications`/`employee_work_history` appear to be its functional successors. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-066 | Phase 0 — new | Double session-ID regeneration on first post-login admin page load | Auth | `auth/login.php`, `auth/session.php` | `login.php` regenerates once at successful auth but never stamps `last_regenerated`; the very next page load through `session.php` regenerates a second time. Harmless, but undocumented and worth a single stamp-on-login fix for clarity. | Open — newly discovered in Phase 0 baseline | Phase 4 |
| KOM-067 | Phase 0 — new | Employee-portal logout omits `session_unset()` before `session_destroy()` | Employee Portal | `employee-portal/logout.php` | Functionally still clears the session store, but skips the explicit in-memory `$_SESSION` unset step that `auth/logout.php` performs — a mechanical inconsistency across the 3 logout implementations (see also KOM-043). | Open — newly discovered in Phase 0 baseline | Phase 4 |

---

## Register Totals

| Severity | Count |
|---|---|
| Critical | 6 |
| High | 18 |
| Medium | 21 |
| Low | 22 |
| **Total** | **67** |

**By status:** 65 Open, 2 Partially-changed-but-still-exploitable (KOM-010, KOM-031), 0 Fixed, 0 Verified, 0 Closed. **No finding in this register has a Date Closed** — this is expected and correct for a Phase 0 baseline artifact.

---

## Change Log for This Document

| Date | Change | Author |
|---|---|---|
| 2026-07-11 | Initial consolidated register compiled from Audit I (34 findings), Audit II (21 findings + 13-item regression check), and Phase 0 baseline inventory (12 newly discovered items) | Remediation Program — Phase 0 |
