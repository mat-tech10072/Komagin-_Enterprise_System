<?php
/**
 * Phase 5, Stage 5.4: infrastructure placeholder.
 *
 * The actual reminder logic (contract expiry, probation expiry,
 * training, recruitment, temp-employee end-date, consultant, document
 * expiry, payroll publication — per the Phase 5 charter's Stage 5.6)
 * is built in Stage 5.6 (Deferred Notification Workflows), which
 * replaces this file's body with the real per-reminder-type logic.
 * Returns 0 until then — a safe, honest no-op, not a failure; the
 * scheduler and its logging are fully wired and ready for that work.
 */
return 0;
